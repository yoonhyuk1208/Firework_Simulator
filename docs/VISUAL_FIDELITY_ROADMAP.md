# 여의도 시각 품질 로드맵

최종 갱신: 2026-09-11<br>
현재 단계: **V2-3b 완료 — 가설 3건 반증, 지표 한계 확인, 개행 정책 결함 수정**<br>
로드맵 상태: **진행 중**

## 1. 목표와 작업 순서

이 로드맵의 일차 목표는 불꽃 공연이 없어도 2024년 10월의 여의도 한강공원이
실사처럼 보이는 3D 공간을 만드는 것이다. 실제 2024-10-05 공연의 발사 위치,
큐시트, 팀별 연출과 시간축은 정적 환경과 일반 불꽃·연기 표현이 목표 품질을
통과한 뒤 결합한다.

고정 우선순위는 다음과 같다.

1. 여의도 지형, 제방, 수변 경계와 표고
2. 잔디, 흙, 포장, 도로와 지표의 미세 형상
3. 건물 외벽, 창호, 옥상과 주요 랜드마크
4. 교량, 수목, 난간, 가로등과 공원 시설물
5. 야간 조명, 그림자, 대기 원근과 화면 안정성
6. 한강 수면, 파랑, 수심 색과 반사
7. 실제 공연과 독립된 단일 불꽃의 형태와 광학 표현
8. 연기 유체, 광산란과 장면 상호 조명
9. 전체 시각 경로의 노트북 60 FPS 안정화
10. 2024-10-05 실제 공연의 위치·시간·연출 결합

앞 단계의 완료 조건을 충족하지 못한 상태에서 뒤 단계의 외관 튜닝으로 문제를
가리지 않는다. 단, 뒤 단계 구현에 필요한 계측·자료 수집·인터페이스 설계는
병렬로 준비할 수 있다.

## 2. 최종 품질 계약

### 시각 품질

- 자유 카메라의 지상 보행 높이와 대표 원경 시점 모두에서 지형이 평면처럼
  보이지 않아야 한다.
- 잔디, 도로, 흙, 콘크리트와 건물 외벽은 색상 패턴만 바뀌는 평면이 아니라
  실제 크기의 거칠기, 법선, 경계 높이와 실루엣을 가져야 한다.
- 63빌딩, IFC, Parc.1, FKI, 국회의사당과 주요 교량은 일반 빌딩 대체물이
  아니라 공개 증거로 확인 가능한 매스, 외장 구성과 야간 특징을 가져야 한다.
- 불꽃은 서브픽셀 단계에서도 방사 궤적이 분리되고 시간에 따라 추진·항력·중력
  영향을 보여야 한다. 밝기 증가는 궤적을 두꺼운 흰 원으로 합치면 안 된다.
- 연기는 밀도·온도·속도장과 광학 두께를 연결하고, 불꽃 빛을 받아 공간적인
  색과 그림자를 형성해야 한다.
- 실제 공연 결합 전까지 모든 데모 불꽃은 명확히 `synthetic`으로 식별한다.

### 성능과 물리 보존

- 기준 해상도는 1280×720, 표시 목표는 60 FPS, 물리 고정 주기는 120 Hz다.
- 노트북 기준 세 번의 독립된 360프레임 통합 측정 모두에서 프레임 p95가
  16.67 ms 미만이어야 해당 시각 단계가 완료된다. p99와 시각·물리 구간 p95도
  함께 기록한다.
- 성능을 위해 지형·건물의 실제 위치, 물리 고정 주기, 유체 보존식이나 압력
  반복을 임의로 낮추지 않는다. 가변 품질은 반사 해상도, 체적 광선 수와 같이
  화면 공간 오차를 측정할 수 있는 항목에만 적용한다.
- 질량, 열에너지, 광원 에너지와 결정성 회귀 검사를 유지한다.

### 증거와 검증

- A/B 등급 측량·사진측량 자료는 실측·재구성 형상에 사용할 수 있다.
- C/D 등급 추정치는 시각적 공백을 메울 수 있지만 파일과 런타임 메타데이터에
  추정임을 남기며, 실제 위치·재질로 표현하지 않는다.
- 등록 가능한 기준 사진은 카메라 재투영 오차를 먼저 통과해야 실루엣·재질
  비교에 사용한다. 포즈가 없는 사진은 색, 명암, 물결과 연기 형태 참고에만
  사용한다.
- 각 단계는 대표 시점의 동일 조건 캡처, 정량 지표, 육안 비교표를 남긴다.

## 3. 전체 진행 현황

진행률은 아래 산출물의 완료 여부를 기준으로 한 공학적 추정치다. 실제 사진과
픽셀 등록된 기준 자료가 부족하므로 외관 유사도 측정값으로 해석하지 않는다.

| ID | 단계 | 상태 | 현재 추정 | 완료 조건 요약 |
|---|---|---:|---:|---|
| V0 | 기준 화면·검증 체계 | 진행 중 | 85% | 대표 카메라, HDR/SDR 캡처, 사진 등록, 품질 점수표 |
| V1 | 지형·제방·수변 형상 | 진행 중 | 87% | 표고·경사·수변 실루엣과 지상 카메라 오차 검증 |
| V2 | 잔디·흙·도로·포장 | 진행 중 | 56% | 실측 스케일 재질, 경계 형상, 근·중·원거리 LOD |
| V3 | 건물 외벽·랜드마크 | 진행 중 | 35% | 주요 건물 사진 정합, 외장·창호·옥상 실루엣 |
| V4 | 교량·수목·시설물 | 진행 중 | 33% | 주요 구조와 증거 기반 배치, 반복 패턴 제거 |
| V5 | 조명·그림자·화면 안정성 | 진행 중 | 35% | TAA, 동적 그림자, 야간 명암과 노출 기준 통과 |
| V6 | 한강 수면·반사 | 진행 중 | 40% | 실제 풍속 기반 파랑, 반사 폭·파단·수변 접촉 검증 |
| V7 | 일반 불꽃 외관 | 진행 중 | 25% | 구경별 궤적·분열·색·발광·블룸 기준 통과 |
| V8 | 연기·광산란 | 진행 중 | 30% | 3D 유체와 광학 두께, 불꽃 조명, 다중산란 근사 |
| V9 | 통합 60 FPS·품질 계층 | 진행 중 | 45% | 세 번의 360프레임 p95 모두 16.67 ms 미만 |
| H1 | 2024 실제 공연 데이터 결합 | 대기 | 5% | 발사 위치·큐시트·영상 시간축과 검증된 전체 재생 |

시각 우선 범위 V0~V9의 현재 종합 진행률은 약 **43%**다. 구조와 물리 기반은
그보다 앞서 있지만, 최종 화면을 결정하는 측량 자료, 외장 정합, 화면 안정화와
연기 조명이 아직 부족하다.

## 4. 단계별 실행 계획

### V0. 기준 화면과 검증 체계

목표: “좋아 보인다”가 아니라 같은 카메라·시간·노출에서 개선을 비교한다.

- [x] 선형 HDR 캡처, 출처 해시와 제한 오차 회귀 비교
- [x] 실제 행사 사진의 출처·노출·사용 범위 기록
- [x] 카메라 포즈 재투영 솔버와 체크섬 게이트
- [x] 지형, 잔디, 도로, 건물, 교량, 수면용 대표 카메라 6종 고정
- [x] 최종 SDR 화면 캡처 명령과 시점별 카메라 상태 격리
- [x] 실루엣 IoU, 엣지 오차, 명암·색 분포와 시간 안정성 보고서
- [x] 실제 사진 또는 영상 최소 3시점의 등록 가능 여부 판정

완료 게이트: 대표 카메라 캡처를 한 명령으로 재현하고, 이후 모든 시각 커밋이
동일한 전후 비교 보고서를 생성해야 한다.

게이트 실측 상태(2026-09-11, V0-16): 조건 1은 **충족**한다. `water_reflection`의
두 잔차는 V0-15에서 독립 재현·음성 대조군까지 확보된 드라이버 거동으로 귀속됐고,
장치·시점·영역·크기를 한정한 공시 허용으로 분리했다. 이 장비에서 10회 비교 전부
통과하며, **허용 픽셀을 뺀 나머지 차이는 0**이다. 조건 2(모든 시각 커밋이 전후
보고서를 생성)는 **도구는 갖췄고 강제는 없다** — hook/CI는 사용자 워크플로 결정이다.

### V1. 여의도 지형, 제방과 수변

- [x] 2024 OSM 경계와 공식 제약을 사용한 1024² 지형
- [x] 한강 수위 기준면과 수면 마스크
- [x] 지형 추종 도로·보행로와 지상 카메라 접촉
- [x] 100m 구간별 급경사·도로 부유·관통·수변 단차 오차 지도
- [x] 행사장 북측 AOI와 도로 테셀레이션·높이장 원인 분리
- [x] AOI 오차 기반 도로·경계석 적응형 L2 세분화와 성능 A/B
- [x] 역사 OSM 터널 중심선·방향 기반 지하 도로 가시성 분리
- [x] 서강대교 공식 상판 하부 EL 기준·제원 체크섬 잠금과 오적용 차단 게이트
- [x] 2024 역사 OSM 서강대교 외곽선 기반 중복 상판 제거와 단일 평면 상판 복원
- [x] 행사일 보도사진·도로공사 목록·공식 도면 기반 비계·도장·종단 등록 가능성 판정
- [x] NGII 서울2447 공개 기준점의 EPSG:5186 축·망실 상태와 적용 범위 감사
- [x] NGII 인증 배포 영수증·패키지/멤버 체크섬 결속과 원자료 불일치 차단
- [ ] NGII/서울 수치지도 원자료의 인증 다운로드와 CRS 검증
- [ ] 제방 마루·사면·옹벽·계단의 증거 기반 단면 복원
- [ ] 수변 가장자리, 호안 블록, 배수구와 토사 접촉부 형상
- [ ] 1 m 이하 표고 오차가 시각에 미치는 구간별 우선순위 지도

완료 게이트: 대표 지상 시점에서 부유 도로, 지형 관통, 계단 단차 역전이 없고,
확보된 측량 단면에 대한 높이 오차가 자료 불확도 안에 있어야 한다.

### V2. 잔디, 흙, 도로와 포장

- [x] 잔디·아스팔트·콘크리트·보도블록 1K PBR 입력
- [x] 바람에 반응하는 잔디 블레이드와 거리 LOD
- [ ] 여의도 현장 사진 기반 재질 색·거칠기·오염 분포 보정
- [ ] 잔디 밀도, 깎인 방향, 맨흙·고사 잔디·수변 습윤 전이
- [ ] 보도 경계석, 균열, 맨홀, 차선·자전거도로 표식의 실제 크기 형상
- [x] 현재 경계석·차선·자전거도로 패턴의 미터 스케일과 증거 결손 감사
- [x] 2024 행사 시점 도로 원본 보존과 way ID·렌더 구간 의미 결속
- [x] 명시적 일방통행·차로 수 구간의 15cm·3m/3m 미터 표식과 추정 표식 제거
- [x] 행사 시점 표식 태그·렌더 way 후보 전수와 유형·배치 독립 차단 게이트
- [x] S-Map 2024 25cm 정사영상 로컬 획득과 EPSG:5186 타일 직접 등록
- [x] 월드 미터 스케일을 보존한 저주파 안티타일링과 명시적 8× 이방성 필터
- [ ] 근거리 패럴랙스/마이크로 디스플레이스먼트
- [ ] 카메라 이동 중 반짝임과 반복 타일 패턴 제거

완료 게이트: 1.68 m 보행 카메라의 2~80 m 범위에서 재질이 평면 또는 반복
타일로 인식되지 않고, 정지·이동 캡처 모두 시간적 깜박임이 없어야 한다.

### V3. 건물 외벽과 랜드마크

- [x] OSM `building:part` 기반 실제 3D 매스
- [x] 63빌딩, IFC, Parc.1, FKI와 국회의사당의 공개 높이·특징 반영
- [x] 절차적 창호, 슬래브, 멀리언, 발코니와 야간 점등
- [x] 외벽 모듈 치수 전수·증거 등급·사진 등록 가능성 감사와 일반 격자 상속 차단
- [ ] 건물별 등록 사진과 외벽 모듈 치수 데이터셋
- [ ] 실측 가능한 외장 패널, 유리 색·거칠기, 기둥과 코너 디테일
- [ ] 옥상 기계실, 안테나, 간판, 로고와 야간 항공등
- [ ] 창문 점등의 층별 군집·색온도·커튼/블라인드 깊이
- [ ] 동적 그림자와 접촉 그림자로 창문 격자와 외벽 질량 분리

완료 게이트: 등록된 원경 사진에서 주요 건물 실루엣과 층·베이 기준점이 허용
오차를 통과하고, 야간에도 창문이 흰 선망처럼 보이지 않아야 한다.

### V4. 교량, 수목과 공원 시설물

- [x] 교량 데크·교각, 난간, 추정 벤치·가로등과 공식 시설 좌표
- [x] 수목 군락과 바람 반응
- [ ] 원효대교·마포대교의 거더, 교각, 난간, 조명과 접속부 정밀 모델
- [ ] 수종·수관 높이·줄기 밀도와 10월 초 단풍 분포
- [ ] 실제 좌표가 확인된 난간, 벤치, 안내판, 매점, 화장실과 운동시설
- [ ] 추정 배치와 실측 배치를 런타임에서 구분하는 품질 마스크

완료 게이트: 교량 실루엣과 반복 간격이 사진 기준을 통과하고, 공원 전경이
비어 있거나 같은 소품의 규칙적 복제로 보이지 않아야 한다.

### V5. 조명, 그림자, 대기와 화면 안정성

- [x] 태양·달·도시광, 물리 카메라, HDR·블룸·시각 적응
- [x] GGX 재질과 화면 공간 주변광 차폐
- [ ] TAA 또는 동등한 시간적 재구성으로 서브픽셀 건물·잔디 안정화
- [ ] 가로등·불꽃용 동적 그림자와 지형·건물 차폐
- [ ] 측정된 등기구 광도 분포·색온도와 건물 외벽의 간접광 근사
- [ ] 대기 에어로졸·연무의 공간 변화와 불꽃 주변 국부 산란
- [ ] 카메라 모드와 인간 시각 모드 각각의 기준 노출·적응 검증

완료 게이트: 카메라 이동 중 창문·난간·잔디가 반짝이지 않고, 건물 외벽과
수목이 검은 컷아웃이 아닌 조명받는 입체로 인식되어야 한다.

### V6. 한강 수면과 반사

- [x] JONSWAP 기반 파랑, Fresnel/GGX, 흡수·산란과 평면 반사
- [x] 수변 마스크, 포말과 불꽃·도시광 반사
- [ ] 행사 시간 풍속·취주거리 기반 스펙트럼 재보정
- [ ] 장주기 흐름, 교각 후류와 국부 바람 변화
- [ ] 실제 영상의 세로 반사 폭, 파단 길이와 밝기 분포 비교
- [ ] 호안 근처 얕은 수심 색, 젖은 경계와 접촉 포말

완료 게이트: 정지 화면뿐 아니라 이동 영상에서 반사가 거울판, 노이즈 또는
균일한 세로 블러로 보이지 않고 기준 영상의 공간·시간 분포를 통과해야 한다.

### V7. 실제 공연과 독립된 불꽃 외관

- [x] 질량·에너지 예산과 구경별 프로파일, 중력·항력·풍속
- [x] 색 변화, 스트로브, 크래클과 물리 카메라 발광
- [ ] 궤적을 선명한 원형 띠로 합치지 않는 서브픽셀 적분·TAA
- [ ] 스타 크기·연소면·광도 감소와 노출에 따른 코어/헤일로 분리
- [ ] 국화·야자·버드나무·링·하트 등 형태별 파편 분포 검증
- [ ] 추진제 꼬리, 지연 신관, 2차 분열과 낙하 잔광
- [ ] 구경·조성별 사진/고속영상 기준 세트

완료 게이트: 합성 단일 발을 다양한 노출에서 촬영해도 방사 궤적과 분열 구조가
유지되고, 밝기만 높여 만든 흰 원 또는 소용돌이로 보이지 않아야 한다.

### V8. 연기 유체와 광산란

- [x] 3D GPU MAC 격자, 질량·열에너지 주입과 압력 투영
- [x] 불꽃·바람·부력에 의한 연기 이동
- [ ] 활성 희소 브릭으로 넓은 발사 영역과 장시간 누적 지원
- [ ] 입자 크기 분포에 따른 소멸·단일산란 계수
- [ ] 불꽃별 연기 직접 조명과 건물·지형 차폐
- [ ] 에너지 보존형 다중산란 근사와 색 오염
- [ ] 실제 영상 광류·연기 실루엣·잔류 시간 검증

완료 게이트: 연기가 회색 투명 텍스처가 아니라 밝은 불꽃 주변에서는 발광하고
뒤쪽에서는 차폐되는 체적으로 보이며, 질량 보존과 p95 게이트를 함께 통과한다.

### V9. 통합 품질과 노트북 60 FPS

- [x] 시각·물리·유체 CPU/GPU p95 세부 계측
- [x] 3D 유체 속도 단계 디스패치 융합
- [ ] 실제 프레임 페이싱과 스터터 원인 타임라인
- [ ] GPU 드리븐 컬링, 인스턴싱과 지형/건물 메시렛 검토
- [ ] 반사·체적광의 지각 오차 기반 품질 계층
- [ ] 대표 카메라 6종과 최악 공연 부하에서 반복 성능 검증

완료 게이트: 품질 기본값에서 세 번의 360프레임 통합 실행 모두 p95 16.67 ms
미만이며, 품질 계층 전환이 물리 상태나 실제 지형을 변경하지 않아야 한다.

### H1. 2024-10-05 실제 공연 결합

이 단계는 V0~V9의 시각 기본 품질이 완료된 뒤 시작한다.

- [ ] 발사 바지선과 발사관 위치
- [ ] 일본·미국·한국 팀의 큐시트 또는 프레임 단위 복원 타임라인
- [ ] 발사별 구경, 고도, 형태, 색, 지연과 동시 발사 그룹
- [ ] 음악 시간축과 광속/음속을 구분한 관측 시간
- [ ] 등록 영상의 불꽃 중심, 반경, 색, 연기와 반사 오차 검증
- [ ] 전체 19:20~20:30 재생과 구간 탐색

완료 게이트: 출처가 확인된 타임라인만 역사 모드에 포함하고, 추정 발사는 별도
불확도와 함께 표시한다. 등록 영상의 대표 구간을 자동 비교해 허용 오차를
통과해야 한다.

## 5. 바로 다음 작업 순서

1. **V0-1 / V1-1:** 지형·잔디·건물·수면 대표 카메라를 고정하고 현재 SDR/HDR
   기준 캡처를 생성한다.
2. **V1-2:** 지상 카메라에서 평면적으로 보이는 제방·호안·계단 구간을 좌표와
   화면 오차로 분류한다.
3. **V1-3:** 확보 가능한 공식 표고·구조물 원자료의 인증 상태와 CRS를 확인하고,
   증거가 있는 구간부터 단면 메시를 교체한다.
4. **V2-2f:** S-Map 2024 영상의 정확한 촬영일과 독립 검사점을 확보한다. 행사일
   이전·1m 이하 오차를 모두 통과한 후보 way만 표식 유형과 종방향 범위를 판독한다.
5. **V3-1:** 주요 건물별 사진 등록 가능성과 외벽 모듈 데이터셋을 구축한다.

실제 공연 타임라인과 바지선 데이터는 이 순서가 끝날 때까지 구현 우선순위에
올리지 않는다. 자료는 발견 즉시 출처만 보존하고 H1 입력 대기열에 둔다.

## 6. 매 턴 진행 업데이트 규칙

앞으로 구현 턴마다 이 문서의 `최종 갱신`, `현재 단계`, 진행 현황과 체크박스를
실제 상태에 맞게 수정한다. 사용자에게 보내는 완료 보고는 반드시 다음 형식을
포함한다.

```text
현재 단계: V?-?
이번 턴 완료: 구현·자료·검증 내용
품질 변화: 전후 화면 또는 정량 지표
성능: frame/visual/physics p95와 측정 조건
검증: 테스트 수와 실패·미구현 지표
남은 위험: 자료 부족 또는 기술 병목
다음 작업: 바로 이어서 수행할 한 단위
커밋: 해시와 메시지
```

상태 정의는 다음과 같다.

- `대기`: 우선순위상 아직 시작하지 않음
- `진행 중`: 일부 산출물이 존재하지만 완료 게이트를 통과하지 못함
- `차단`: 필요한 외부 자료나 권한 없이는 정직하게 진행할 수 없음
- `완료`: 자료·시각·성능·회귀 게이트를 모두 통과함

## 7. 커밋 규칙

- 하나의 검증 가능한 작업 단위를 하나의 커밋으로 남긴다.
- 구현과 그 구현의 테스트·문서·측정값은 같은 커밋에 포함할 수 있다.
- 여러 시각 영역을 한 커밋에서 임의로 섞지 않는다.
- 점검만 수행하고 파일을 바꾸지 않은 턴에는 빈 커밋을 만들지 않는다.
- 커밋 메시지는 한국어 Conventional Commit을 사용한다.

예시:

```text
feat: 지형 기준 카메라와 시각 회귀 캡처 구현
feat: 제방 단면과 호안 접촉 형상 정밀화
feat: 잔디 근거리 미세 형상과 시간 안정화 구현
perf: 수목·잔디 GPU 컬링 최적화
fix: 연속 연소 연기의 보존적 격자 주입 수정
docs: 시각 품질 로드맵 진행 상태 갱신
```

## 8. 진행 기록

각 프로젝트 턴은 아래에 한 줄 이상의 결과를 누적한다. 코드 변경이 없는 점검
턴도 판단 결과와 다음 우선순위를 기록하되, 문서만 갱신하기 위한 빈 커밋은
만들지 않는다.

### 2026-08-03 — 로드맵 기준선 수립

- 현재 단계: V0 기준선 고정, V1 지형·수변 형상 정밀화 준비
- 완료: 시각 품질 감사를 바탕으로 V0~V9와 H1의 순서·산출물·완료 게이트 확정
- 기준 화면 판단: 정적 지리·물리 기반과 야간 하늘은 존재하지만 지형 근경,
  외벽 질량, 불꽃 궤적, 연기 조명과 시간적 안정성이 사진 수준에 미달
- 성능 기준: 직전 최종 코드의 360프레임 반복에서 물리 p95 5.096/5.498 ms,
  전체 프레임 p95 16.000/18.516 ms로 안정적인 60 FPS는 아직 미달
- 검증 기준: 전체 521개 테스트 통과, 검증 지표 실패 0; 시각 핵심 지표 다수는
  등록 기준 자료 부재로 `NO_REFERENCE`
- 다음 작업: V0-1/V1-1 대표 지형 카메라와 재현 가능한 SDR/HDR 캡처 세트 구현

### 2026-08-03 — V0-1/V1-1 대표 시점 회귀 기준선

- 현재 단계: V0 대표 화면 기준선 확보, V1 지형·수변 시각 오차 분류 준비
- 완료: 지형·잔디·도로·건물 외벽·교량·수면 6개 고정 시점, 장면 자산 체크섬,
  지표면 종류와 카메라 여유 높이 게이트, 선형 HDR NPY와 최종 SDR PNG 일괄 캡처 구현
- 품질 변화: 렌더 외관 자체는 변경하지 않았으며, 1280×720 `human_vision` 화면에서
  비유한값과 완전 검정·흰색 클리핑은 6시점 모두 0%; 잔디 형상 부재, 반복 창문,
  평면적인 수변·도로가 후속 개선의 고정 결함 기준으로 확인됨
- 결정성: 독립 실행 간 HDR 4화소, SDR 5화소의 GPU 경계 표본 차이를 확인했다.
  파일 SHA-256은 출처 확인에 사용하고, 회귀 게이트는 변경 화소 0.001% 이하,
  HDR 최대 절대오차 0.001과 평균오차 1e-7 이하를 동시에 요구함
- 성능: 캡처 도구는 오프라인 전용이라 런타임 렌더 경로와 기존 p95를 변경하지 않음;
  6시점 독립 렌더(각 8프레임)는 이 장비에서 총 120.2초
- 검증: 대표 시점·표면·체크섬·HDR/SDR 저장·제한 오차 비교 집중 테스트 75개와
  전체 회귀 테스트 531개 통과
- 남은 위험: 고정 시점은 결함 노출용 C등급 공학 카메라이며 실제 행사 관람자 또는
  촬영자 위치가 아님. 사진 등록과 실제 관찰 위치 확정 전에는 현실 정합도 판정에 사용 불가
- 다음 작업: V1-2 고정 지형·수변 화면을 구간별로 분해해 부유·관통·경사·접촉 오차 지도 작성

### 2026-08-03 — V1-2 지형·수변 접촉 오차 지도

- 현재 단계: V1-2 오차 지도 완료, V1-3 P0 구간의 자료 오류와 렌더 표현 오류 분리 준비
- 완료: 1024² 지형 경사, 물-육지 인접 단차, 도로 삼각형 중심·변 중점의 지형 추종
  편차와 미분류 수면 횡단을 검사하고 100m 구간별 P0/P1/P2 지도·JSON 보고서 생성
- 품질 변화: 렌더 외관은 변경하지 않았으며, 고정 화면에서 보이던 평면·접촉 문제를
  좌표로 추적할 수 있게 됨. 38도 이상 비수변 급경사 2,313표본, 4m 이상 수변 단차
  311표본, 목표 6cm에서 20cm 이상 벗어난 육지 도로 표본 13,511개를 확인
- 우선순위: 영향 구간은 P0 271개, P1 261개, P2 360개. 도로-수면 미분류 표본은
  427개이며 교량, 마스크 오류, 잘못 연결된 도로로 후속 분류가 필요함
- 성능: 분석기는 오프라인 전용이며 실제 장면의 지도·JSON 생성은 이 장비에서 3.5초;
  런타임 렌더 경로와 기존 frame/visual/physics p95는 변경하지 않음
- 검증: 평면 도로 무오류, 내부 지형 봉우리 관통, 수변 단차, 미분류 수면 횡단,
  지도 래스터 정합과 임계값 검증 집중 테스트 6개 및 전체 회귀 테스트 537개 통과
- 남은 위험: 표본 수는 삼각형 공유 변에서 같은 위치를 중복할 수 있으며 측량 오차가
  아닌 렌더 표현 결함 지표다. P0를 실제 지형 오류로 단정하기 전에 원자료 대조가 필요함
- 다음 작업: V1-3 P0 구간에서 높이장 급경사와 도로 삼각형 테셀레이션 오차를 분리하고,
  행사장에 직접 보이는 북측 수변부터 보정 대상을 확정

### 2026-08-03 — V1-3 행사장 P0 원인 분리

- 현재 단계: V1-3 원인 분리 완료, V1-4 적응형 도로 지형 추종 보정 준비
- 완료: 공식 공원시설 9개와 회귀 카메라 6개를 포함한 C등급 행사장 북측 AOI를
  고정하고, 원본 높이장을 바꾸지 않는 도로 삼각형 L0/L1/L2 중점 분할 반사실험 구현
- 품질 변화: 렌더 외관 자체는 변경하지 않았지만 AOI의 20cm 이상 도로 편차율이
  L0 0.8800%에서 L2 0.0349%로 96.03% 감소함을 확인. 현재 우선 표본 403개는
  모두 38도 미만 높이장에 있어 이 범위의 주원인을 메시 밀도 부족으로 분류
- 수변 판단: 북쪽에 물이 바로 인접한 행사장 수변 348표본의 높이는 중앙값 2.210m,
  p95 2.360m, 최대 2.654m이며 4m 이상 단차는 없음. 전역 P0 수변을 행사장 오류로
  잘못 일반화하지 않도록 보정 범위를 분리함
- 성능: L2 전체 적용은 삼각형 16배로 노트북 목표에 부적합. 분석기는 오프라인 전용이며
  실제 장면 L0~L2 분석·지도 생성은 5.3초, 런타임 p95는 변경하지 않음
- 검증: AOI 장면 체크섬·카메라·공식 시설 포함, 삼각형 분할, 고정 높이장 편차 감소,
  북측 수변 방향성과 지도 정합 집중 테스트 12개 및 전체 회귀 테스트 543개 통과
- 남은 위험: L2 뒤에도 국소 최대 1.172m와 20cm 이상 표본 256개가 남는다. 표본 수는
  분할마다 증가하므로 절대 개수가 아니라 비율로 비교해야 하며 잔류 구간은 급곡률·수변
  마스크·도로 연결 형상을 추가 판정해야 함
- 다음 작업: V1-4 현재 편차가 큰 AOI 삼각형만 최대 L2로 나누는 적응형 지형 추종 메시를
  구현하고, 정점 증가량·회귀 화면·360프레임 p95를 함께 검증

### 2026-08-03 — V1-4 적응형 도로·경계석 지형 추종

- 현재 단계: V1-4 적응형 보정 완료, V1-5 잔류 램프·교량 의미 분류 준비
- 완료: 행사장 AOI의 지형 접촉 편차가 5cm 이상인 일반 도로와 경계석 삼각형만
  최대 L2로 나누고, 계단·수면 횡단·AOI 밖 메시를 그대로 유지하는 런타임 배치 통합
- 품질 변화: 일반 도로 최대 편차 2.043→1.172m, p99 0.177→0.114m,
  50cm 이상 표본 125→35개. 결함 시점의 SDR 209,793화소(22.7640%)가 바뀌며
  상판·차선·경계석이 같은 곡면을 따르도록 수정
- 형상 비용: 도로 70,464→76,236삼각형, 경계석 81,024→86,553삼각형.
  계단 포함 총 정점은 455,112→489,015개로 1.0745×이며 전역 L2 16×를 회피
- 기준 화면: 기존 도로 카메라가 수정 위치를 보지 못해 최대 편차를 직접 보는 보행
  시점으로 교체. 캡처 manifest에 카메라 정의 SHA-256을 추가해 다른 시점끼리 비교 금지
- 성능: 부모/현재 교대 360프레임 3회에서 frame p95는 부모
  16.975/17.452/17.084ms, 현재 16.910/17.290/16.991ms. 성능 악화는 관측되지
  않았지만 현재도 세 번 모두 16.67ms 미만이 아니므로 안정적 60FPS 게이트는 실패
- 검증: 적응 조건·AOI·수면 게이트·속성 보간·실장면 정점 상한·카메라 체크섬과
  A/B 도구 집중 테스트 45개 및 전체 회귀 테스트 551개 통과
- 남은 위험: 잔류 최대 1.172m 구간은 램프·교량·급곡률 높이장이 섞여 있다. 세분화는
  접촉을 개선하지만 도로 구조의 실제 분류나 높이를 증명하지 않으며 화면은 아직 실사 미달
- 다음 작업: V1-5 잔류 20cm 이상 표본을 OSM 교량·램프 의미, 수변 마스크 근접,
  높이장 급곡률로 나눠 잘못 지형 추종하는 구조부터 분리

### 2026-08-03 — V1-5 지하 도로 의미 분리와 잔류 구조 분류

- 현재 단계: V1-5 의미 분리 완료, V1-6 교량·램프·포털 표고 증거 확보 준비
- 완료: 2024-10-05 10:20 UTC 역사 OSM에서 AOI의 `tunnel`·음수 `layer/level`
  도로 8개를 별도 증거 자산으로 고정하고, 같은 중심선 0.25m 이내이면서 방향 코사인이
  0.98 이상인 기존 노면만 런타임 배치에서 제외. 교차하는 지상 도로는 보존
- 품질 변화: 지형 위에 잘못 중복되던 지하 도로 125세그먼트를 제거. 적응형 L2 뒤
  AOI의 20cm 이상 접촉 표본은 256→185개, 50cm 이상은 35→32개, p99는
  0.1135→0.0941m. 동일 공학 카메라 SDR 1,834화소(0.1990%)에서 의도한 변화 확인
- 잔류 분류: 185개 중 157개는 기존 교량 중심선 35m 이내, 5개는 수변 25m 이내,
  18개는 15도 이상 높이장, 5개는 미분류. 교량 인접은 실제 상판·램프 표고를
  증명하지 않으므로 공개 A/B등급 단면 자료 없이 임의 상승·평탄화하지 않음
- 형상 비용: 일반 도로 입력 삼각형 70,464→70,214개, 적응형 출력은
  76,236→75,731개로 감소. 필터는 장면 초기화에서 한 번만 실행
- 성능: 부모/현재 교대 360프레임 3회에서 현재 frame p95는
  14.844/19.830/15.075ms로 2/3회만 16.67ms 미만. 한 회의 시각·물리 동시
  이상치 때문에 엄격한 전 실행 60FPS 게이트는 실패하며 성능 개선을 인과적으로 주장하지 않음
- 검증: 중심선·방향·교차 도로 보존·실장면 8개 way 정합 집중 테스트 41개와
  전체 회귀 테스트 555개 통과, 의미 분류 지도·JSON·고정 시점 A/B를 보존
- 남은 위험: 최대 1.172m 편차는 주로 서강대교·국회대로 접속부에 남아 있다.
  터널 깊이, 포털, 옹벽, 교량 접근 램프의 실제 표고와 단면은 아직 복원되지 않음
- 다음 작업: V1-6 공개 도로·교량 종단면, 수치지도 구조선 또는 등록 가능한 사진으로
  서강대교 접속부의 상판·램프·포털 표고를 확보하고 체크섬 잠금 증거 게이트를 통과

### 2026-08-03 — V1-6 서강대교 공식 수직 기준과 오적용 차단

- 현재 단계: V1-6 공식 기준 고정 완료, V1-7 완공 종단면 station 등록 준비
- 공식 증거: 서울시 정밀안전진단·정밀점검 원문과 서울시의회 공개 표 세 건을
  PDF/HWP 렌더링으로 확인하고 SHA-256 잠금. 본교 1,320m·폭 29~34m, 북단
  접속교 386m·폭 29m, Ramp-A 411.6m·폭 7.5m와 대표 횡단면 깊이 2.035m 기록
- 수직 기준: 항로부 상판 하부 `EL 23.3m`를 장면 EL 기준 2.79m에 맞춰
  `runtime Y=20.51m`로 변환. 표의 정밀도는 0.1m이나 측정 불확도와 정확한
  종단 station은 원문에 없으므로 불확도 값을 임의로 만들지 않음
- 안전 게이트: A/B 출처·체크섬, station 기준점 2개 이상, 평면 RMSE 0.25m,
  상면 EL 또는 하부 EL+구조 깊이, 수직 불확도 0.10m 조건을 코드·JSON 스키마로
  강제. 공개 스캔이 조건을 충족하지 못해 현재 적용 상태는 명시적으로 차단
- 시각·성능 영향: 장면 정점 수정 0개, 배포 NPZ 체크섬은 이전과 동일. 런타임
  프레임 경로를 바꾸지 않아 프레임 시간 예상 변화 0ms이며 새 p95 개선을 주장하지 않음
- 검증: 수직 기준 변환·오적용 거부·등록 프로필 허용·RMSE/불확도 실패·체크섬·
  장면 무변경 집중 테스트 9개, 전체 회귀 테스트 564개와 기계 판독 감사 보고서 통과
- 남은 위험: 현재 모든 OSM 교량 정점은 지형 상대 7m의 일반화된 값이고 항로부
  station과 실제 종단 형상이 없다. 공식 자료도 2012·2015·2019년 작성본이므로
  2024-10-05까지 구조 변경이 없었는지 확인해야 하며 `EL 23.3m`를 상판 상면으로
  직접 쓰면 안 됨
- 다음 작업: V1-7 시설물 변경 이력을 먼저 확인하고 완공도면의 종단 station 두
  점 이상을 2024 역사 OSM 중심선에 등록한 뒤, 고해상도 도면/사진측량 교차검증으로
  상판 상면 EL 표본을 확보

### 2026-08-03 — V1-7 서강대교 역사 평면 의미 복원

- 현재 단계: V1-7 평면 중복 제거 완료, V1-8 행사일 구조 상태·종단면 등록 준비
- 역사 자료: 2024-10-05 10:20 UTC OSM의 서강대교 외곽선·차도·보도 way ID
  7개와 선택 payload SHA-256을 고정. 폐합 외곽선 양 변을 65개 횡단면으로
  재표본화해 연결된 단일 상판 64세그먼트 생성
- 형상 변화: 외곽선 내부/8m 이내에 겹치던 일반 상판 90세그먼트를 제거하고
  64세그먼트로 대체. 교량 배치는 6,330→6,174정점, 누적 평면 면적은
  119,112.65→58,036.61㎡. 입력·출력 수직 좌표는 모두 `Y=7m`
- 등록 실패: 외곽선 중심선 1,766.024m와 공식 본교+북단 접속교 1,706m 사이
  잔차가 60.024m이므로 OSM 평면 거리를 완공도면 station으로 대용하지 않음.
  `EL 23.3m` 상판 하부 기준과 실제 종단 형상 적용은 계속 차단
- 2024 시점 위험: 서울시 도장공사 기간이 행사일을 포함하지만 당일 공정률·비계
  위치·새 도장 경계는 공개되지 않았다. 근거 없는 임시 구조물과 도장 경계를 생성하지 않음
- 시각 검증: 동일 공학 카메라 HDR·SDR A/B는 0픽셀 변화. 최상단 실루엣은
  유지하고 중복 스트립만 제거한 결과이므로 외관 향상을 주장하지 않음
- 성능: 현재 360프레임 frame p95 21.202/23.620/21.424ms로 3회 모두
  16.67ms 초과. 부모는 14.873/19.451/20.472ms로 1회만 통과했고, 순차 실행의
  시각·물리 동시 상승 때문에 이번 메시 축소의 인과적 회귀는 확정하지 못함
- 검증: 의미 자산·중복 선택·연결성·다른 장면 오적용 거부 집중 테스트 6개와
  전체 회귀 570개 통과. 기계 판독 보고서·평면 지도·고정 시점 A/B 보존
- 다음 작업: V1-8 행사일 전후 고해상도 사진으로 도장·비계 상태를 판정하고,
  완공도면의 식별 가능한 교각/신축이음 기준점을 역사 평면에 0.25m RMSE 이하로
  등록한 뒤 상판 상면 EL 종단 표본을 확보

### 2026-08-03 — V1-8 서강대교 행사일 상태와 종단면 등록 증거 감사

- 현재 단계: V1-8 증거 감사 완료, V1-9 인증 수치지도·측량 기준 확보 준비
- 행사 사진: Newsis·News1·Wikitree·파이낸셜뉴스의 날짜가 확인되는 원본 4개를
  체크섬 잠금하고 원본 해상도로 검토. 독립 시점은 3개지만 모두 주 발사 수역과
  여의도 동측 스카이라인을 향해 서강대교 식별 시점은 0개
- 공사 상태: 서울시 도장공사는 행사일에 진행 중이고 공중비계 14,556㎡를 포함.
  행사일 서울경찰청 도로공사 목록에는 서강대교 작업이 없지만 이는 교통 통제
  상판 작업에만 적용하며 하부 비계 부재의 증거로 확대하지 않음
- 공식 도면: 체크섬이 기존 자산과 일치하는 2015 HWP에서 원본 임베디드 도면을
  추출. P8-P9 닐센 아치 150.000m는 확인했으나 독립 기준점은 2개뿐이고 본교
  종단 래스터는 최소 0.5m/px로 0.125m/px 게이트에 미달
- 구조 이력: 2019 공식 보고서 4~5쪽에서 준공도면·보수보강 이력 보유와 주요
  설계변경 내역 없음을 확인. 이는 2019년까지만 유효해 행사일까지의 불변성은 미확정
- 강화된 게이트: station 기준점 최소 3개, 도면 0.125m/px 이하, A/B등급 평면,
  0.25m 이하 RMSE, 행사일까지 구조 이력 검증을 모두 요구. 기존 두 점 합성 fixture도
  세 점으로 교체해 두 점 완전 적합으로 RMSE가 인위적으로 0이 되는 문제 차단
- 적용 결과: 비계·새 도장 경계·station·수직 프로필 모두 차단, 장면 정점 수정
  0개, 런타임 프레임 경로 변화 0개. 집중 회귀 20개와 전체 회귀 581개 통과
- 다음 작업: V1-9 NGII 또는 서울시 인증 수치지도에서 P8/P9 외 제3 교각·신축이음
  구조선과 EL 기준을 확보하고 CRS·축척·라이선스를 검증해 station 등록 재시도

### 2026-08-03 — V1-9 NGII 공개 기준점과 CRS 축 검증

- 현재 단계: V1-9 공개 기준점 감사 완료, V1-10 인증 2024 이전 1:1,000 배포본 확보 준비
- 도엽 상태: 국토정보플랫폼 서울2447(`376082447`)의 1:1,000 DXF는 제작연도
  2025, 좌표체계 표시는 `GRS80`, 이전 이력정보 없음. 비로그인 상태에서 제품을
  선택하고 다운로드를 실행했지만 다운로드 이벤트와 파일은 확인되지 않음
- 공개 기준점: 공공삼각점 CP01·CP02·CP11·NO22의 공식 성과 메타정보에서
  좌표 참조 코드 5186, 평면좌표, 정표고를 확보. 네 점 모두 4급이며 상태 `망실`,
  서강대교 교각·신축이음과 직접 결속되는 구조 기준점은 0개
- 축 검증: 포털 `minx/miny`를 Easting/Northing으로 전달하고 `always_xy=True`로
  EPSG:5186→WGS84 변환하면 네 점 모두 장면 경계 안에 위치. 축 교환 시 네 점
  모두 장면 밖이므로 포털 필드 매핑과 EPSG 네이티브 축 순서를 별도로 고정
- 안전 게이트: 망실 기준점은 현장 기준·station·표고 적용을 금지하고, 인접
  기준점의 EPSG 코드를 미확보 DXF에 전이하지 않음. 2025 후행 도엽과 파일
  메타데이터 미확인 상태도 별도 차단
- 적용 결과: 카탈로그 CRS 참고만 허용, 수치지도 CRS·현장 기준·station·수직
  프로필은 모두 차단. 장면 정점 수정 0개, 런타임 프레임 경로 변화 없음
- 검증: 기준점 상태·축 순서·CRS 범위·SHA-256 연결·과잉 적용 차단 집중 테스트
  18개와 전체 회귀 591개 통과, Python 컴파일 검사 통과
- 다음 작업: V1-10 인증 세션에서 2024년 또는 이전 1:1,000 배포본과 라이선스·
  투영 CRS 메타데이터를 확보하고, 서강대교 구조점에 결속되는 현존 기준점 세 개
  이상으로 0.25m RMSE station 등록 재시도

### 2026-08-03 — V1-10a 인증 배포물 인수·무결성 게이트

- 현재 단계: V1-10a 로컬 인수 게이트 완료, V1-10b 공식 포털 로그인 배포본 확보 대기
- 공식 경로 재확인: 서울시 안내는 1:1,000 DXF/NGI를 국토정보플랫폼에서
  제공하며 다운로드에는 로그인이 필요하다고 명시. 내장 브라우저는 비로그인,
  연결 가능한 외부 Chrome 세션은 없어 인증 배포물 0개
- 인수 계약: 행사 AOI 네 도엽을 명시하고 현재 필수 도엽을 서울2447로 고정.
  제작연도 2024 이하, 명시적 배포물 EPSG, 로컬 파생 사용 허용 라이선스,
  패키지와 추출 멤버의 SHA-256·바이트 수를 모두 요구
- 바이트 결속: 구조물 가져오기 CLI가 영수증에 기록된 전체 DXF 해시·크기
  멀티셋과 실제 입력을 비교. 파일명 변경은 허용하지만 누락·추가·중복·변조된
  바이트 스트림은 모두 차단
- 증거 분리: 공개 기준점 EPSG:5186과 카탈로그 `GRS80` 표시는 배포물 CRS
  증거로 사용할 수 없고, 내장 메타데이터·제공자 sidecar·제공자 영수증만 허용
- 현재 판정: 안전 게이트 통과, V1-10 완료는 거짓. 인증 다운로드·2024 이전
  제작연도·CRS·라이선스·서울2447 커버리지가 모두 없어 `stage_complete=false`
- 적용 결과: 장면 정점 수정 0개, 런타임 프레임 경로 변화 없음. 따라서 화면과
  프레임 시간에는 변화가 없으며 출처가 없는 시각 개선도 없음
- 검증: 배포 영수증 스키마, 과잉 승인, CRS 추정, 중복 해시, DXF 변조와 정확
  일치 경로를 포함한 집중 회귀 27개와 전체 회귀 600개 통과, Python 컴파일
  검사 통과
- 다음 작업: V1-10b 인증 세션에서 서울2447의 2024년 또는 이전 공식 배포본과
  제공자 CRS·라이선스 증거를 확보해 영수증을 채우고, 감사 통과 후 구조선을
  정규화한다. 별도로 서강대교 현존 구조 기준점 세 개는 계속 필요

### 2026-08-03 — V1-10b 서울2447 현행·이력 배포 감사

- 현재 단계: V1-10b 포털 배포 감사 완료, V1-10c 2024 이전 보관본 확보 준비
- 공식 포털 확인: 여의도 검색 결과의 수치지형도 DXF 목록에서 서울2447
  (`376082447`)은 제작연도 2025, 좌표체계 표기 `GRS80`, 축척 1:1,000인
  현행 항목 하나만 제공됨
- 이력 확인: 서울2447의 공식 `이력보기` 응답은 `이력정보가 없습니다.`이며,
  공개 제품 목록에는 2024년 또는 이전 1:1,000 서울2447 배포본이 없음
- 인증 상태: 확인 세션은 비로그인 상태이고 로그인 팝업은 인앱 브라우저에서
  열리지 않았으나, 로그인 전 제품·이력 카탈로그 자체가 요구 연도의 후보를
  노출하지 않으므로 로그인만으로 V1-10을 완료할 수 있다고 가정하지 않음
- 안전 판정: 2025 현행본을 2024-10-05 행사 지형으로 소급 적용하지 않고,
  카탈로그의 `GRS80` 표기도 배포 파일의 투영 EPSG 증거로 승격하지 않음.
  영수증은 계속 `stage_complete=false`, 장면 정점 수정은 0개
- V2 병행 판단: 저장소의 포장 PBR은 일반 CC0 스캔이고 행사일 기사 사진에는
  위치·자세·실척이 결속된 여의도 포장 표본이 없어, 근거 없는 색·거칠기 보정을
  수행하지 않음
- 다음 작업: 국토지리정보원 오프라인 신청 또는 공식 자료 문의로 2024년 이전
  서울2447 보관 배포본의 존재와 제공 조건을 확인한다. 확보 시 제공자 CRS·
  라이선스 sidecar와 패키지/멤버 체크섬을 영수증에 결속하고 감사를 재실행한다.
  병행 가능한 V2 작업은 위치·촬영 자세·실척이 기록된 포장 표본 확보부터 시작

### 2026-08-03 — V1-10c 과거 배포본 신청 경로 준비

- 현재 단계: V1-10c 공식 문의 초안 완료, 사용자 인증·제출 대기
- 인증 게이트: 포털 로그인은 정부 통합인증의 모바일 신분증·간편인증 또는
  아이디 로그인을 요구한다. 서울2447 선택 후 오프라인 신청도 비로그인
  인앱 세션에서는 신청서로 전환되지 않아 개인정보를 대신 입력하거나 제출하지 않음
- 신청 산출물: 도엽번호·축척·제작연도 상한·DXF/NGI·명시적 EPSG·연구용 로컬
  파생 이용조건·공식 인수증을 요청하는 `ARCHIVE_REQUEST.md`를 추가. 신청자
  이름·연락처·소속은 저장소에 기록하지 않도록 분리
- 안전 판정: 문의와 수령 전 장면 정점 수정 0개, 영수증은
  `stage_complete=false`. 2025 현행본과 `GRS80` 카탈로그 표기를 행사일
  구조와 투영 EPSG의 대체 증거로 사용하지 않음
- 다음 작업: 사용자가 공식 포털에서 인증한 뒤 문의 초안을 검토·제출하고,
  제공기관 회신이나 패키지를 받으면 원본을 버전 관리 밖에 보관한 채 제공자
  메타데이터와 체크섬을 영수증에 결속해 V1-10 감사를 재실행

### 2026-08-03 — V1-10d 2025 인증 배포본 채택·구조선 정규화

- 채택 결정: 프로젝트 소유자의 명시적 지시에 따라 서울2447·2448·2457·2458의
  2025 공식 배포본을 정규화와 로컬 파생 장면 작업에 사용한다. 단,
  `historical_identity_claim=false`로 고정하여 2024-10-05 당시와 동일하다고
  주장하지 않는다
- 원본 무결성: 다운로드 폴더의 DXF 4개, 수치지도 2.0 ZIP 4개, XML 8개 등
  원본 16개의 파일명·SHA-256·바이트 수를 인증 영수증에 고정. 원본은 저장소에
  복사하거나 재배포하지 않음
- 좌표계: 각 ZIP의 제공자 PRJ가 동일한
  `Korea 2000 / Central Belt 2010`을 명시하며 pyproj 판독 결과는
  `EPSG:5186`. 카탈로그 `GRS80` 또는 인근 기준점에서 추정하지 않음
- 정규화 입력: 수치지도 2.0 SHP의 `C0050000`(제방), `F0030000`(성·절토),
  `F0040000`(옹벽) 멤버 8개를 체크섬과 결속해 사용. DXF의 스타일 접미 코드
  해석에 의존하지 않음
- 결과: `assets/yeouido_ngii_structures.json`에 구조선 71개, 평면 길이
  3,560.145m를 생성. 제방 15개, 성·절토 6개, 옹벽 50개이며 장면 좌표 범위는
  East -441.99~442.01m, South -111.00~933.15m
- 수직 게이트: 선택 SHP에는 고도 정보가 없어 71개 모두 높이를 `null`로 유지.
  옹벽 높이·상하단·성토 사면을 추정하지 않으며 장면 정점 수정은 계속 0개
- 검증: 인증 인수·사후 자료 명시 승인·전체 도엽 커버리지·EPSG·패키지/멤버
  체크섬·스키마·정규화 집중 테스트 18개 통과. 감사 결과
  `safety_gate_passed=true`, `stage_complete=true`,
  `historical_fidelity_complete=false`
- 다음 작업: V1-11에서 구조선에 대응하는 공인 표고·단면·측량 또는 사진측량
  수직 프로파일을 확보하고, 행사일 이후 변화 여부를 분리 검증한 뒤에만
  `tools.merge_ngii_structures`로 장면 형상을 수정

### 2026-08-03 — V1-11a NGII 상대 높이·상하단 속성 감사

- DXF 수직 검사: 4개 도엽의 `C005/F003/F004` 계열 구조선 Z 좌표를 전수
  검사했으나 모두 `0.0`으로, 실제 표고가 아닌 2D 자리표시자로 판정
- DBF 증거: 수치지도 2.0 ZIP의 SHP/SHX/DBF/PRJ 멤버를 패키지 체크섬과
  결속하고 정규화 구조선 71개 전부를 UFID·레코드와 일대일 연결
- 속성 결과: 상단 62개, 하단 9개, 상하구분 미상 0개. 높이 필드가 있는
  구조선은 65개이고 양수 상대 높이는 59개, 범위는 0.3~8.0m
- 의미 제한: DBF `높이`는 구조물의 상대 치수이며 서울 EL 절대 표고 또는
  런타임 Y가 아니다. `높이`를 지형고에 단순 가산하거나 상단선을 곧바로
  옹벽 상단 표고로 취급하지 않음
- 산출물: `assets/yeouido_ngii_structure_attributes.json`에 원본 멤버 해시,
  feature ID, UFID, 상하단, 상대 높이와 적용 차단 사유를 고정
- 장면 영향: 절대 수직 앵커가 없으므로 `mesh_merge_allowed=false`, 장면
  정점 수정 0개 유지
- 다음 작업: V1-11b에서 상단·하단 선의 공간적 쌍을 검증하고 공인 DEM 또는
  측량 표고에 결속하는 수직 앵커 규칙을 설계한다. 2024-10-05 이후 변화
  검증과 오차 한계를 통과한 피처만 장면 병합 후보로 승격

### 2026-08-03 — V1-11b 등고선·표고점 수직 앵커 적합성 감사

- 추가 원본 발견: 이미 확보한 수치지도 2.0 ZIP에 `F0010000` 등고선 35개와
  `F0020000` 표고점 12개가 포함됨. SHP/SHX/DBF/PRJ 멤버를 기존 인증
  패키지 체크섬과 결속해 별도 다운로드 없이 수직 근거 후보로 감사
- 표고 범위: 제공 속성의 절대 표고 후보는 2.977~23.84m. 등고선을 2m
  간격으로 재표본화해 2,825개 제약점을 만들었고 구조선 정점의 88.03%가
  등고선+표고점 볼록껍질 안에 위치
- 상하단 평면 매칭: 하단선 9개를 같은 종류의 상단선과 양방향 거리로 비교.
  중앙값 2.5m 이하, p95 3.0m 이하, 길이비 2.0 이하를 동시에 만족한 강한
  평면 후보는 4쌍
- 교차검증 실패: 표고점 12개 중 등고선 볼록껍질 내부는 5개뿐이며,
  등고선 보간 대비 표고점 오차는 RMSE 4.027m, MAE 2.794m,
  p95 7.215m. 0.3~8.0m 구조 높이를 앵커링하기에 부족
- 기존 지형 한계: 1:5,000 기반 지형의 제약 적합 RMSE 2.488m,
  p95 4.033m와 약 4.89×3.91m 출력 간격도 협폭 제방·옹벽의 상하단을
  분리하기에 부족
- 판정: 평면 대응 후보 4쌍도 절대 수직 오차 0.10m 게이트를 통과하지 못해
  `vertical_anchor_pass_count=0`, `mesh_merge_allowed=false`, 장면 정점 수정 0개
- 다음 작업: V1-11c에서 명시적 수직 기준면과 오차가 제공되는 고해상도 DEM
  또는 구조물 측량 표고를 확보한다. 평면 RMSE 0.25m 이하·수직 불확도 0.10m
  이하를 검증한 피처만 병합 대상으로 승격

### 2026-08-03 — V1-11c 2024 공개 DEM 후보 선택·인수 게이트

- 공식 후보: 국토정보플랫폼의 여의도 검색 결과에서 공개DEM 6개
  (2014·2021·2022·2023·2024·2025)를 확인. 행사 연도와 일치하는
  `2024 서울 37608` 한 건을 선택
- 범위 판단: `37608`은 기존 1:1,000 구조 도엽 네 장을 포함하는 상위 서울
  도엽 후보이며, 상세 커버리지·격자 간격·수직 기준면은 실제 배포물의
  메타데이터로 재검증해야 함
- 인증 상태: 새 Chrome 탭의 포털 세션은 만료되어 다운로드 클릭 시
  `해당 서비스는 로그인 후 이용하실 수 있습니다.` 팝업으로 전환. DEM 선택
  탭과 로그인 탭을 사용자 인계 상태로 유지
- 인수 명세: `assets/yeouido_ngii_dem_request.json`에 도엽·연도·필수 CRS·
  수직 기준면·격자 간격·NoData·체크섬 및 0.25m/0.10m 검증 게이트를 기록
- 안전 판정: 인증 패키지를 받기 전 `scene_merge_allowed=false`, 장면 정점
  수정 0개. 카탈로그 항목만으로 해상도나 수직 정확도를 추정하지 않음
- 다음 작업: 사용자가 유지된 Chrome 로그인 탭에서 인증한 뒤 연결 완료를
  알리면 선택된 2024 DEM을 다운로드한다. 원본은 저장소 밖에 보관하고
  패키지·메타데이터를 체크섬 고정한 후 실제 격자와 수직 오차를 감사

### 2026-08-03 — V1-11c Chrome 재인증·DEM 신청서 준비

- 인증 확인: 사용자의 연결 완료 후 DEM 탭을 새로고침해 `로그아웃`과
  `마이페이지`가 표시되는 인증 세션을 확인
- 신청 준비: `2024 서울 37608`을 다시 선택하고 사용목적을
  `제작 및 활용 / 교육, 학술, 연구`, 상세용도를 프로젝트 지형 고도 검증·
  시각화 연구로 입력. 사용자 준수사항은 기존 상시 동의 지시에 따라 동의
- 개인정보 게이트: 신청서가 계정 이름과 필수 생년월일 입력을 요구한다.
  생년월일을 추정하거나 계정·브라우저 저장소에서 조회하지 않고 사용자가 직접
  입력하도록 작성 중인 DEM 신청서 탭을 인계 상태로 유지
- 현재 판정: 인증과 비개인 신청 항목은 완료됐으나 제출 전이므로 원본 파일 0개,
  장면 정점 수정 0개, `scene_merge_allowed=false`
- 다음 작업: 사용자가 Chrome 신청서에서 생년월일을 입력한 뒤 완료를 알리면
  최종 다운로드를 실행하고 체크섬·CRS·수직 기준면·격자·NoData를 감사

### 2026-08-03 — V1-11c DEM 제출 시도·포털 초기화 진단

- 제출 결과: 사용자가 필수 생년월일을 직접 입력한 완성 신청서를 두 차례
  제출했으며, 두 경우 모두 신청 창은 닫혔지만 Chrome 다운로드 이벤트와
  `Downloads` 폴더의 신규 파일은 확인되지 않음
- 진단 결과: 다운로드 이벤트를 직접 대기해도 60초 안에 이벤트가 발생하지
  않았고 포털 콘솔에는 이 요청과 관련된 오류가 남지 않음. 신청 창을 다시
  열면 사용목적·상세용도·약관이 초기화되며, 네트워크 추적을 위해 재개방한
  마지막 회차에는 생년월일도 초기화됨
- 개인정보 처리: 생년월일 값은 저장소·검증 산출물·로그에 기록하지 않았으며,
  비개인 신청 항목과 약관 동의만 다시 채워 입력 화면을 사용자에게 인계
- 현재 판정: 원본 DEM 파일 0개, 장면 정점 수정 0개,
  `scene_merge_allowed=false`. 파일이 실제 생성되기 전까지 CRS·수직 기준면·
  격자 간격·NoData·체크섬 판정은 보류
- 다음 작업: 사용자가 열린 Chrome 신청서의 생년월일 한 칸을 다시 입력하고
  완료를 알리면, 이미 시작한 네트워크 추적으로 실제 요청 URL·응답 상태를
  확인하면서 최종 제출한다. 파일이 생성되면 즉시 저장소 밖에서 메타데이터와
  수직 정확도를 감사

### 2026-08-03 — V1-11c 공개 DEM API 결함·공식 메타데이터 감사

- 자동 재입력: 사용자가 이후 양식 입력을 위임함에 따라, 사용자가 입력한
  생년월일은 현재 Chrome 세션 안에서만 재사용하고 저장소·로그에는 기록하지
  않은 채 공식 `?tabGb=total` 진입 경로까지 신청을 반복
- 서버 결함 확인: 포털 `download.js`는
  `/pd/shbtManage/shbtInsertTritVidoDem.do`에서 JSON `orderDownList`를 기대하지만,
  실제 응답은 HTTP 200 `text/html`의 `요청하신 페이지를 찾을 수 없습니다`
  화면. 마이페이지 공개 DEM 건수도 0이므로 신청 대기나 전송 프로그램 단계가
  아니라 제공자 백엔드 경로 결함으로 판정
- 공식 대체 증거: 공공데이터포털에서 로그인 없이 NGII 기본메타데이터와
  수치표고성과내역 CSV를 다운로드하고 각각 SHA-256을 고정. 원본 CSV는
  저장소 밖 `Downloads`에 유지하고 감사 결과만
  `docs/validation/ngii_dem_v1/ngii_dem_metadata_report.json`에 기록
- 공간 규모: 프로젝트 bbox `126.910–126.960°E`, `37.515–37.545°N`은
  EPSG:5186 기준 약 4.42 km × 3.33 km. 2009 항공 LiDAR 1m 후보 중
  `서울087–089`, `서울097–099` 여섯 도엽이 전체 범위와 겹침
- 수직 메타데이터: 여섯 도엽 모두 인천항 평균해수면 기준 정표고, ASCII,
  Upper-Left 기록 순서를 명시. 다만 정확도 필드가 전부 비어 있고 실제 래스터
  헤더·NoData·체크섬을 확보하지 못했으므로 0.10m 수직 불확도 게이트는 실패
- 현재 판정: 2024 DEM 원본 0개, 장면 정점 수정 0개,
  `scene_merge_allowed=false`
- 다음 작업: 포털 복구 후 `2024 서울 37608`을 재시도하거나, 공식 기관에
  2009 1m 여섯 도엽의 직접 제공 경로와 정확도 문서를 요청한다. 래스터 확보
  전에는 기존 지형을 교체하지 않음

### 2026-08-03 — V1-11d 공식 DEM 제공 요청 패키지

- 공식 경로: 현재 제공되지 않거나 정상 다운로드되지 않는 공공데이터를 요청하는
  공공데이터포털 `공공데이터 제공신청` 경로를 1차 제출 경로로 고정
- 우선 요청: 행사 연도와 일치하는 공개DEM `2024 서울 37608` 원본 패키지 또는
  정상 다운로드 경로를 요청하도록 한국어 문의 초안을 준비
- 대체 요청: 2009 항공 LiDAR 1m 후보 `서울087–089`, `서울097–099` 여섯
  도엽을 정확한 도엽번호와 함께 고정하고, 빠진 도엽이 있으면 감사를 실패 처리
- 필수 증빙: 래스터 원본, 투영 EPSG, 수직 기준면·높이 종류, 격자·NoData,
  제작/취득일, RMSE·LE90 또는 동등한 정확도 문서, 체크섬/인수증, 로컬 파생
  사용과 재배포 조건을 모두 요구
- 개인정보·외부 작업: 신청자 이름·연락처·생년월일은 저장소에 기록하지 않으며,
  현재 산출물은 `ready_for_manual_submission=true`이지만 실제 외부 제출은 수행하지 않음
- 안전 판정: 원본 배포와 품질 증빙을 받기 전 `scene_merge_allowed=false`, 장면
  정점 수정 0개. 평면 RMSE 0.25m 이하·수직 불확도 0.10m 이하를 계속 요구
- 다음 작업: 사용자가 `PROVIDER_REQUEST.md`를 검토해 공식 계정으로 제출하고
  신청 번호·회신·패키지를 받으면 저장소 밖 원본의 체크섬과 래스터 헤더를 감사한다.
  제공기관이 정확도 문서를 제공하지 않거나 0.10m 게이트를 충족하지 못하면 구조물
  수직 병합에는 사용하지 않음

### 2026-08-03 — V1-11e 공공데이터포털 제출 승인·인증 인계

- 제출 승인: 프로젝트 소유자의 다음 단계 진행 지시를 공식 제공신청 제출 승인으로
  반영. 요청 본문과 대상 도엽·필수 증빙은 V1-11d 체크섬 잠금본을 그대로 유지
- 인증 분리: NGII에 로그인된 Chrome 세션과 공공데이터포털 계정은 공유되지 않음.
  제공신청서 작성 버튼은 별도 로그인을 요구하며 정부 통합인증(Any-ID)의 간편인증·
  공동인증서·금융인증서·민간ID 또는 공공데이터포털 아이디 로그인을 제시
- 사용자 인계: Chrome을 공공데이터포털 로그인 선택 화면에 유지. 비밀번호·인증서·
  인증 코드 등은 조회하거나 저장소에 기록하지 않고 사용자가 직접 인증하도록 차단
- 현재 판정: `submission_payload_ready=true`,
  `external_submission_authorized=true`, `external_submission_performed=false`,
  `portal_authentication_pending=true`. 장면 정점 수정 0개 유지
- 다음 작업: 사용자가 열린 Chrome 탭에서 인증을 완료하고 연결 완료를 알리면,
  준비된 요청 본문을 입력해 최종 제출하고 접수 화면의 비개인 신청번호·처리기관·
  접수시각을 증거로 고정한다

### 2026-08-03 — V1-11f 포털 아이디 로그인·CAPTCHA 인계

- 인증 재확인: 사용자의 다음 단계 진행 지시 후 유지된 Chrome 탭을 다시 확인했으나
  정부 통합인증 선택 화면에 그대로 있어 인증 완료 상태가 아님을 확인
- 로그인 경로: 공공데이터포털 `아이디 로그인`을 선택해 공식 인증 페이지로 이동.
  화면은 포털 아이디·비밀번호와 자동등록방지 문자 입력을 필수로 요구
- 보안 차단: 저장된 비밀번호를 조회하거나 자격증명을 추정하지 않았고 CAPTCHA도
  사용자 확인 없이 해석·입력하지 않음. 로그인 화면을 Chrome에 인계
- 현재 판정: 제출 본문은 준비되고 외부 제출은 승인됐지만 인증과 CAPTCHA는
  미완료, 실제 제출 0건. 개인정보·자격증명 저장 0건, 장면 정점 수정 0개
- 다음 작업: 사용자가 열린 Chrome 탭에서 아이디·비밀번호·자동등록방지 문자를
  입력해 로그인한 뒤 연결 완료를 알리면 신청서 작성·최종 제출·접수 증거 고정을 진행

### 2026-08-03 — V1-11g 공식 제공신청서 본문 입력·개인정보 인계

- 인증 완료: 사용자가 Chrome에서 공공데이터포털 로그인을 완료했고 공식
  `공공데이터 제공신청서 작성` 화면으로 정상 전환됨을 확인
- 개방여부 확인: 포털 검색은 NGII DEM·수치표고성과내역·기본메타데이터가 이미
  제공 중이라고 표시하지만, 실제 2024 래스터 신청 API 결함과 미배포 상태를
  설명하는 제공신청은 계속 작성 가능
- 신청 내용: 데이터명을 `여의도 2024 공개 DEM 직접 제공 및 수치표고 정확도 문서`로
  입력하고 담당 기관을 `국토교통부 국토지리정보원`, 활용 목적을 학술연구로 선택.
  우선 2024 서울37608과 대체 2009 1m 여섯 도엽·CRS·수직기준·NoData·정확도·
  체크섬·이용조건을 요구하는 본문 976자를 입력
- 개인정보 차단: 신청서의 생년월일·주소·전화번호·CAPTCHA는 비워 두고, 개인정보
  수집·이용 및 제3자 제공 동의도 선택하지 않음. 화면은 개인정보를 접수·처리기관에
  제공하고 10년간 보유한다고 명시하므로 사용자가 내용을 확인한 뒤 직접 완료하도록 인계
- 현재 판정: 인증과 비개인 신청 항목은 완료, 실제 제출 0건, 개인정보 저장 0건,
  장면 정점 수정 0개
- 다음 작업: 사용자가 열린 Chrome 신청서에서 필수 개인정보·주소 검색·CAPTCHA를
  입력하고 두 동의 항목을 확인한 뒤 완료를 알리면 최종 제출과 접수번호 증거 고정을 진행

### 2026-08-03 — V1-11h 신청자 필수 입력 검증·동의 재인계

- 입력 검증: 사용자의 입력 완료 후 값 자체는 읽거나 출력하지 않고 생년월일·주소·
  전화번호·CAPTCHA 네 필드의 비어 있지 않음만 검사했으며 모두 완료 상태
- 신청 내용 재검증: 데이터명과 담당 기관이 준비본과 정확히 일치하고 본문 976자,
  학술연구 목적 및 상세 목적 122자가 유지됨을 확인
- 동의 상태: 개인정보 수집·이용과 제3자 제공의 `동의함` 라디오가 모두 미선택.
  10년 보유·접수 및 처리기관 제공에 대한 동의를 추정하거나 대신 선택하지 않음
- 현재 판정: 신청자 필수 필드·CAPTCHA 완료, 두 동의 미완료, 실제 제출 0건,
  개인정보 저장 0건, 장면 정점 수정 0개
- 다음 작업: 사용자가 열린 Chrome 탭에서 두 `동의함`을 직접 선택하고 완료를
  알리면 동일 요청 내용과 동의 상태를 최종 확인한 뒤 제출·접수번호 고정을 진행

### 2026-08-03 — V1-11i 공식 DEM 제공신청 접수

- 최종 검증: 두 개인정보 동의가 모두 선택됐고 생년월일·주소·전화번호·CAPTCHA가
  비어 있지 않으며, 데이터명·NGII 담당 기관·본문 976자·학술연구 목적이 준비본과
  일치함을 값 노출 없이 확인
- 제출 결과: 공공데이터포털이 마이페이지 `result=success`로 전환되고 요청 ID
  `136949`를 생성. 접수기관 `국토교통부 국토지리정보원`, 신청일 `2026-08-03`,
  포털 표시 처리일 `2026-08-14`, 현재 상태 `접수대기중`
- 개인정보 처리: 신청자 정보와 CAPTCHA는 저장소·로그·접수 감사에 기록하지 않고,
  요청 ID·기관·일자·상태만 비식별 접수 증거로 고정
- 현재 판정: `external_submission_performed=true`,
  `submission_receipt_valid=true`, `provider_review_pending=true`. 래스터 원본은
  아직 0개이므로 `scene_merge_allowed=false`, 장면 정점 수정 0개 유지
- 다음 작업: 포털 상태가 접수완료·처리중·제공완료로 바뀌거나 NGII 회신이 도착하면
  요청 ID와 결속해 회신·원본 패키지를 인수한다. 래스터 헤더·CRS·수직 기준·NoData·
  정확도·이용조건·체크섬이 모두 검증되기 전에는 기존 지형을 교체하지 않음

### 2026-08-03 — V2-1a 실척 재질·이동 반짝임 기준선

- 작업 전환: NGII DEM 요청은 외부 심사 대기로 분리하고, 원본 인수 전 지형 병합을
  계속 금지한 채 독립적으로 수행 가능한 V2 재질 계측으로 이동
- 실척 감사: 일반 CC0 1K 스캔 4종의 타일 폭·텍셀 피치·가장자리 불연속과 24 mm,
  1280×720, 눈높이 1.68 m 카메라의 2·5·10·20·40·80 m 지면 픽셀 풋프린트를 기록
- 이동 기준선: `grass_close`, `road_ground`에서 카메라 오른쪽으로 프레임당 0.02 m
  이동하는 선형 HDR 8프레임을 각각 캡처. 평균 프레임 델타는 각각
  1.017e-6, 3.755e-6이고 신호 p95 정규화 값은 0.00395, 0.00448
- 런타임 판정: 월드 미터 UV·미프맵·삼선형 축소 필터는 활성화됐지만 스캔 텍스처
  배열의 이방성 필터는 명시되지 않음. 80 m 지면 풋프린트 이방성은 52.5:1이며,
  단일 타일은 2~80 m 검사 구간에 재질별 19.5~41.1회 반복
- 증거 제한: 네 스캔 모두 여의도 표본이 아닌 D등급 일반 재질이고 등록된 실제 이동
  영상의 허용 임계값도 없으므로 현장 색 보정과 반짝임 통과 게이트는 계속 비활성
- 검증 산출물: `docs/validation/material_detail_v1/material_detail_report.json`과
  `material_motion_diagnostic.png`; 동일 조건 재생성 도구와 계산·보고서 회귀 테스트 추가
- 다음 작업: V2-1b에서 지원 범위 안의 이방성 필터를 명시하고 저주파 좌표 변형 또는
  재질별 변형 레이어로 반복을 완화한 뒤, 같은 두 이동 시퀀스로 전후 정량·화면 A/B 수행

### 2026-08-03 — V2-1b 재질 필터링·안티타일링 A/B

- 필터 구현: 스캔 텍스처 배열에 8× 이방성 필터를 명시하고 현재 OpenGL 드라이버의
  `ARB/EXT_texture_filter_anisotropic` 지원과 실제 적용값 8.0을 런타임에서 확인.
  확장 미지원 장치에서는 1×로 안전하게 유지
- 반복 완화: 타일의 미터 폭은 그대로 두고 최대 0.16m의 교차축 저주파 변형과
  비주기적 매크로 명도 ±6%·거칠기 ±0.0275 레이어를 적용. 좌표 자코비안으로 계산한
  국소 스케일 변화 상한은 1.136%
- 동일 조건 A/B: V2-1a와 같은 24mm·1280×720·시점당 HDR 8프레임·프레임당 0.02m
  이동을 재생. `grass_close` 델타는 동일했고 `road_ground`의 평균/정규화 델타 비는
  1.090/1.086, 고주파 델타 비는 1.029
- 정직한 판정: 보존된 표면 대비와 시간적 반짝임을 등록 실제 영상 없이 분리할 수
  없으므로 반짝임 개선 또는 V2 완료를 선언하지 않음. 필터 적용·스케일 보존·동일 조건
  A/B 재현만 완료하고 현장 색 튜닝 및 시간 안정성 게이트는 계속 비활성
- 성능: 1280×720, 3D 유체, GPU 완료 대기 360프레임 단일 실행에서 frame p95
  16.250ms, visual p95 12.297ms, physics p95 5.484ms. 60 FPS 예산은 통과했지만
  V9의 독립 3회 게이트를 대신하지 않음
- 검증 산출물: `docs/validation/material_filtering_v2/material_filtering_report.json`,
  `material_filtering_ab.png`, 재생성 도구와 셰이더·상수·보고서 회귀 테스트
- 다음 작업: V2-2에서 보도 경계석·차선·자전거도로 표식·맨홀의 현재 실척과 증거
  등급을 감사한 뒤, 확인 가능한 형상부터 메시/데칼 단위 구현 대상으로 고정

### 2026-08-03 — V2-2a 경계석·도로 표식 실척 감사

- 렌더 인벤토리: 터널 가시성 필터 이후 선형 구간 35,215개, 누적 중심선
  353.87km. 아스팔트 25,616개/262.96km, 보행로 6,846개/63.27km,
  자전거도로 2,726개/27.39km, 비포장 산책로 27개/0.25km
- 경계석 판정: 아스팔트 원본 구간 10,072개에서 높이 0.14m·상단 폭 0.18m,
  원점 1.7km 이내 조건으로 파생. 치수와 적용 반경은 여의도 실측이 아닌 D등급
  일반값이므로 현장 검증 완료로 승격하지 않음
- 차선 폭 결함: 셰이더의 정규화 횡좌표 때문에 아스팔트 폭 3.20~28.80m에 따라
  동일 표식의 물리 폭이 0.144~1.296m로 변함. 0.20m 초과 19,965구간,
  0.30m 초과 15,475구간이며 도로별 차로 수·중앙선 종류와 연결되지 않음
- 자전거도로 판정: 현재 패턴은 9.09m 주기의 폭 1.00m 횡방향 밴드이고 자전거
  기호 형상·현장 위치 증거는 없음. 맨홀·균열 역시 장면 형상과 날짜 증거 모두 0개
- 출처 결손: `yeouido_scene.npz`는 도로 원본 way ID와 태그를 보존하지 않으며,
  별도 상세 OSM 스냅샷 131개 요소에는 highway·맨홀·자전거 표식 요소가 모두 0개.
  터널 의미 자산은 아래등급 가시성만 다루므로 차선·표식 근거로 확장하지 않음
- 구현 범위: 기존 수치를 이름 있는 상수로 노출해 화면값을 바꾸지 않고 감사 가능하게
  했으며, 새 경계석·맨홀·균열·표식 배치는 0개. 증거 결속 전 형상 적용 게이트는 실패
- 검증 산출물: `docs/validation/road_detail_v1/road_detail_audit.json`,
  `road_detail_scale_map.png`, 미터 폭 복원·상수·보고서 회귀 테스트
- 성능: 감사 코드는 오프라인 전용이고 런타임 메시·드로 호출·셰이더 수식 값은 동일.
  직전 V2-1b의 360프레임 frame p95 16.250ms 계약을 변경하지 않음
- 다음 작업: V2-2b에서 2024-10-05 10:20 UTC의 highway 원본을 별도 체크섬 파일로
  보존하고 way ID·태그·중심선을 장면 구간과 결속해, 근거 있는 도로만 미터 표식을 허용

### 2026-08-03 — V2-2b 행사 시점 도로 의미 결속

- 원본 보존: 2024-10-05 10:20 UTC의 동일 bbox에서 OSM `highway` way 2,421개를
  전체 반환 태그·형상·질의·ODbL 표기와 함께 별도 스냅샷으로 저장하고 SHA-256 고정
- 결속: 장면 생성 당시 도로 포함 정책과 현재 검증된 터널 차폐 필터를 재현하고,
  중심선 끝점의 방향 무관 1cm 서명으로 가시 도로 35,215구간을 대조
- 결과: 35,203구간(99.966%)을 단일 way ID에 고유 결속, 원본 미대응 0구간.
  같은 중심선을 공유하는 보행자 way 5개의 12구간은 중복 후보로 남겨 자동 채택 차단
- 태그 범위: 명시 폭 52 way, 차로 수 189 way, 표면 437 way,
  `cycleway:right` 52 way, `bicycle` 162 way, `turn:lanes` 0 way
- 안전 게이트: `yeouido_scene.npz`와 런타임 렌더 코드는 변경하지 않았고,
  `runtime_marking_application_allowed=false`를 유지해 의미 결속을 표식 생성과 분리
- 검증 산출물: `assets/yeouido_road_osm_2024-10-05.json`,
  `assets/yeouido_road_detail_semantics_2024-10-05.json`,
  `docs/validation/road_semantics_v2/road_semantics_binding_map.png`, 체크섬·중복·결속 회귀 테스트
- 성능: 이번 단계는 오프라인 자산과 로더만 추가하며 실행 시 소비자가 없어 직전
  360프레임 frame p95 16.250ms 계약을 변경하지 않음
- 다음 작업: V2-2c에서 고유 결속 구간 중 명시 태그가 있는 도로만 선택해 화면 폭과
  무관한 미터 단위 차선·가장자리 표식 계약을 만들고, 태그 부재 구간은 무표식 유지

### 2026-08-03 — V2-2c 근거 기반 미터 도로 표식

- 적용 게이트: 고유 결속 아스팔트 중 `oneway=yes`와 정수형 `lanes>=2`가 모두
  명시된 66 way만 같은 방향 내부 차로 경계 대상으로 허용
- 적용 결과: 4,387구간·중심선 48.57km에 차로 경계 9,933개를 생성하고,
  근거가 부족한 아스팔트 21,229구간의 기존 전역 가장자리선·중앙선을 제거
- 물리 치수: 행사 직전 도로교통법 시행규칙 별표 6과 국토교통부 2024 차선도색
  유지·관리 매뉴얼을 근거로 폭 0.15m, 도색 3m·빈 구간 3m를 월드 미터로 복원
- 미적용 범위: 양방향 중앙선의 색·실선/점선, 가장자리선, 회전 화살표와 자전거
  기호는 역사 배치 근거가 없어 차단. 근거 없는 자전거도로 횡단 띠 패턴도 제거
- 런타임 검증: 전용 근접 공학 카메라에서 15cm 점선의 원근·주기와 무표식 구간을
  확인하고 OpenGL 3.3 실제 드라이버 셰이더 컴파일 통과
- 성능: 1280×720, 3D GPU MAC 유체, GPU 완료 대기 360프레임 단일 실행에서
  frame p95 13.475ms, visual p95 10.696ms, physics p95 4.099ms로 60 FPS 예산 통과
- 검증 산출물: `docs/validation/road_markings_v2/metric_road_marking_report.json`,
  `metric_road_marking_map.png`, `runtime_current/metric_lane_close.sdr.png`, 재생성 도구와 회귀 테스트
- 다음 작업: V2-2d에서 공식 노면표시 유형과 행사 시점 도로 사진을 way별로 등록해
  양방향 중앙선·가장자리선·화살표·자전거 기호를 독립 게이트로 확장

### 2026-08-03 — V2-2d 도로 표식 유형·배치 증거 감사

- 원본 전수: 2024-10-05 10:20 UTC OSM에서 `lanes` 189 way,
  `lanes:forward/backward` 각 6 way, 자전거 차로형 태그와 `crossing:markings`를
  전수하고 현재 렌더 구간에 way ID로 결속
- 후보 범위: `oneway=yes`가 아닌 다차로 중앙선 검토 37 way·502구간·5.14km,
  방향별 차로 수 5 way·103구간·1.12km, 자전거 차로 태그 31 way·694구간·7.41km,
  횡단 표식 존재 3 way·6구간·0.04km를 공간 지도로 분리
- 차단 근거: 원본에 `divider`, `shoulder`, `turn:lanes*`, `road_marking`,
  `lane_markings`, 중앙선 유형 태그가 없고 공식 규정은 way별 행사일 배치를 지정하지
  않으므로 새 중앙선·가장자리선·화살표·자전거 기호·횡단 형상 적용은 모두 보류
- 영상 근거 판정: 서울 S-Map은 2024 배경지도 연도 선택을 지원하지만 획득일,
  재사용 범위, 원본 해상도와 월드 등록 잔차가 아직 없어 픽셀 증거로 채택하지 않음
- 품질 변화: 새 런타임 형상 0건. 기존 V2-2c 차로 경계 66 way·4,387구간을 유지하며
  근거 없는 후보의 오적용을 회귀 게이트로 차단
- 성능: 오프라인 감사·지도·테스트만 추가해 직전 1280×720 360프레임의 frame p95
  13.475ms, visual p95 10.696ms, physics p95 4.099ms 측정값을 변경하지 않음
- 검증 산출물: `docs/validation/road_marking_evidence_v2/road_marking_evidence_report.json`,
  `road_marking_evidence_map.png`, 입력·지도 체크섬과 후보 수량·차단·등록 계약 회귀 테스트
- 다음 작업: V2-2e에서 S-Map 2024 영상의 획득일과 사용 범위를 확인하고 최소 제어점
  4개·독립 검사점 2개, 평면 잔차 1.0m 이하로 후보 크롭을 way별 등록

### 2026-08-03 — V2-2e S-Map 2024 영상지도 좌표 등록

- 브라우저 검증: S-Map에서 배경지도 `2024`, 3D 건물 비활성화와 고화질을 선택하고
  여의도한강공원 상세 요청이 `ortho_drone_25cm_2024` JPEG·HTTP 200임을 확인
- 원시 좌표 계약: S-Map `mapConst.js`의 EPSG:5186, 원점 `[153468, 513944]`,
  0.25m/px, 256px 타일 계약과 설정 소스 SHA-256을 고정. 제공자 격자를 직접 써서
  별도 GCP 피팅 없는 북향 world file 생성
- 로컬 획득: zoom 10 타일 36장, 1,536×1,536px·384m×384m 크롭을
  `local_reference/smap_2024_ortho/`에 내려받고 36개 개별·모자이크 체크섬을 기록
- 후보 결속: 크롭 안의 자전거 차로 태그 3 way(`384826214`, `474507374`,
  `474507381`)·렌더 34구간을 EPSG:5186 픽셀 범위에 결속
- 권리 분리: 공식 가이드는 현재 지도 PNG 저장을 지원하지만 사이트는 서울특별시
  모든 권리 보유를 표시하고 원시 타일 재배포 허용은 확인되지 않아 픽셀은 Git 제외
- 차단 유지: 설정 주석 `[202410]`과 `20241209`는 정확한 촬영일이 아니며 독립
  검사점도 없어 2024-10-05 행사일 표식 판독과 런타임 형상 변경은 0건
- 품질 변화: 추정 사진 등록에서 25cm 제공자 격자 직접 등록으로 전환하고 자전거도로
  후보의 실제 픽셀 검토 범위를 384m 정사 크롭으로 고정
- 성능: 런타임 소비자 없이 로컬 획득 도구·오프라인 보고서만 추가해 직전
  frame/visual/physics p95 13.475/10.696/4.099ms 측정값을 변경하지 않음
- 검증 산출물: `docs/validation/road_marking_registration_v2/`, 로컬 모자이크·world
  file, 타일 수학·체크섬·권리 분리·행사일 차단 회귀 테스트
- 다음 작업: V2-2f에서 제공자에게 2024 영상의 정확한 촬영일을 확인하고, 모자이크의
  횡단보도·교차로 모서리 독립 검사점으로 1m 이하 등록 오차를 검증

### 2026-08-04 — V2-2f 획득 재현성 복구와 검사점 실행 가능성 감사

- 환경 결함: 선언된 지리 의존성 `requirements-terrain.txt`(pyproj·pyshp·scipy)가
  이 작업 환경에 설치돼 있지 않았다. `tools/acquire_smap_ortho.py`를 포함한 6개 도구가
  pyproj를 필수로 요구하므로 V2-2e 산출물을 재실행·재검증할 수 없는 상태였다.
  설치 후 pyproj 3.7.2가 EPSG:5186을 `KGD2002 / Central Belt 2010`으로 해석함을 확인
- 정정: 최초 점검에서 "pyproj가 어디에도 선언되지 않았다"고 보고했으나 이는 오류다.
  `requirements-dev.txt`가 `-r requirements-terrain.txt`로 포함하고 있으며 선언은
  정상이었다. 실제 결함은 선언이 아니라 미설치였다
- 회귀 규모: 의존성 설치 후 전체 테스트가 650개 통과·2개 건너뜀으로 실행된다.
  설치 전 이 환경에서의 통과 수를 측정하지 않았으므로 증가분은 주장하지 않는다
- 재현성 검증: 보고서에 기록된 체크섬과 로컬 픽셀을 대조해 타일 36/36, 모자이크,
  world file, manifest가 모두 SHA-256 일치함을 확인. 획득 도구가 무조건 재다운로드하므로
  재실행 대신 체크섬 대조로 검증해 제공자 서버에 불필요한 요청을 보내지 않음
- 등록 계약 검증: world file이 곧 등록이므로(제공자 격자 직접 사용, GCP 피팅 없음)
  픽셀 중심 기준 world file과 보고서 bbox `194044/547224~194428/547608`의 상호
  일치를 새 회귀로 고정. 0.25m/px와 남향 음수 픽셀 크기도 함께 검사
- V2-2f(b) 판정 — **자료 차단**: 보유 자료로 1m 이하 독립 검사점 검증이 불가능하다.
  NGII 공개 기준점 4개는 전부 `망실`이라 영상에서 식별할 수 없고, 좌표상으로도 크롭
  서쪽 390.4/397.1/528.1/702.0m로 전부 범위 밖이다. OSM 평면 형상은 이 프로젝트
  자신의 V-05 근거상 1~3m 정확도라 1m 게이트를 검증할 수 없고, NGII 구조선 71개는
  2025년 사후·고도 없음·선형 자료라 점 검사점을 제공하지 않는다
- V2-2f(a) 판정 — **사용자 게이트**: 정확한 촬영일은 제공자 문의가 필요하며 V1-10c의
  NGII 신청과 동일한 처리가 맞다. 설정 주석 `[202410]`·`20241209`는 촬영일이 아님
- 품질 변화: 런타임 형상 0건, 장면 정점 수정 0개. 행사일 표식 분류 게이트는 계속 닫힘
- 성능: 렌더 경로 무변경으로 직전 frame/visual/physics p95 13.475/10.696/4.099ms 유지
- 검증: 신규 집중 테스트 6개(체크섬 전수, 픽셀 미커밋, world file↔bbox 일치, 행사일
  게이트 폐쇄)와 전체 회귀 650개 통과, 2개 건너뜀
- 남은 위험: 로컬 픽셀은 Git 밖이라 다른 장비에서는 체크섬 검증이 건너뛰어진다.
  크롭을 기준점 쪽으로 옮겨도 망실 기준점은 영상 식별이 불가능하므로 근본 해결이 아니다
- 다음 작업: S-Map 제공자에게 2024 정사영상의 정확한 촬영일과 재사용 범위를 묻는 문의
  초안을 `ARCHIVE_REQUEST.md` 패턴으로 작성하고, 사용자 인증·제출 후 회신을 영수증에 결속

### 2026-08-04 — V2-2f(a) S-Map 정사영상 촬영일·이용범위 문의 패키지

- 제출 경로 실측: S-Map Q&A 게시판(`https://smap.seoul.go.kr/guide/qna.html`)이
  비회원 글쓰기를 제공하고 `담당부서` 열과 관리자 `[Re]` 답변 이력(1~2일)을 가짐을
  브라우저로 확인(2026-08-04T05:04:12Z). 담당은 공지 `448061`이 명시한 공간정보과와
  공표 번호 `02-2133-2835`. 경로를 추정하지 않고 실제 화면에서 확인한 값만 기록
- 신규 제공자 약관 증거 — 공지 `448061`(2025-12-03, 공간정보과): 서울시가 저작권을
  보유한 범위에서 S-Map **지도 화면 캡처 이미지**를 저작권법 제24조의2에 따라 별도
  허락 없이 상업적·비상업적으로 이용 가능하며 출처 표시 `출처 : 서울시 S-Map
  (https://smap.seoul.go.kr)`가 필수. 일부 레이어의 외부기관 자료는 해당 기관 정책 적용
- 위 공지를 포괄 허가로 읽지 않은 두 가지 이유를 그대로 기록: (1) **범위 한계** —
  공지 대상은 화면 캡처이고 V2-2e가 받은 픽셀은 타일 서비스에서 직접 받은 타일이라
  공지가 다루지 않는 경우다. (2) **표기 불일치** — 본문은 상업적 이용을 허용하는데
  게시판은 그 게시물에 공공누리 제4유형(상업적이용금지·변경금지)을 표시한다.
  추정으로 해소하지 않고 질문 4로 제공자에게 확인한다
- Q&A 공지 `441460`(2025-08-18)의 3D 제공 FAQ·학술연구 신청 서식·보안심사 안내는
  맥락으로만 기록. 3D 자료 제공 절차라 배경 정사영상과 별개이고, 첨부 3건은
  파일명·바이트 수만 남기고 **내려받지 않았다**(외부 파일 다운로드는 사용자 게이트)
- 요청 패키지: `assets/yeouido_smap_ortho_provider_request.json`에 질문 4개를 각각
  열려 있는 게이트에 결속. 촬영일·모자이크 구성, 저작권 보유 주체, 위치정확도 성과
  문서와 정사보정 기준면, 타일 로컬 연구 이용 범위
- 감사 도구 `tools/audit_smap_ortho_provider_request.py`의 핵심 불변식은
  `gate_classification_complete`다. V2-2e가 공표하는 모든 게이트를 제공자 응답 가능·
  하위 결과·상태 표식 중 하나로 분류하지 않으면 패키지를 차단한다. 이후 단계가 새
  게이트를 추가했을 때 조용히 무시되는 경로를 막는 것이 목적이며, 미분류 게이트
  주입으로 실제 차단됨을 테스트로 실증
- **하한 보장**: 촬영일 회신만으로는 행사일 표식 분류가 열리지 않는다. 감사는
  (a) 대상 범위의 **가장 늦은** 촬영일 ≤ 2024-10-05, (b) 독립 검사점 잔차 ≤ 1.0m
  문서화, (c) 타일 로컬 연구 이용 확인을 모두 요구한다. 모자이크가 가장 오래된
  프레임으로 통과하는 경로를 (a)가 막는다. 유리한 회신을 넣어도 (b)가 없으면 계속
  차단됨을 테스트로 확인
- 미수신 상태를 위조하지 않음: 회신 스키마 `assets/smap_ortho_provider_reply.schema.json`
  만 정의하고 회신 파일은 커밋하지 않는다. 감사는 회신 부재를 유일한 차단 사유로 보고
- 결함 발견·수정: `jsonschema`가 어떤 requirements에도 선언돼 있지 않았다. 이 환경에는
  무관한 패키지(`kaggle-environments`, `litellm`)의 전이 의존으로만 설치돼 있었고,
  직전 실행의 건너뜀 2건이 바로 이 스키마 검증 테스트였다. `requirements-dev.txt`에
  선언을 추가
- 새 가드 `tests/test_declared_test_dependencies.py`: `importorskip`이 지키는 모듈은
  전부 requirements에 선언돼야 한다. 선언을 지우면 영향받는 테스트 모듈 3개를 이름으로
  지목하며 실패함을 실증해 비공허성을 확인. 미매핑 모듈도 실패 처리해 다음 누락을 막는다
- 품질 변화: 없음. 런타임 형상 0건, 장면 정점 수정 0개. 행사일 표식 분류 게이트 폐쇄 유지
- 성능: 렌더 경로 무변경으로 직전 frame/visual/physics p95 13.475/10.696/4.099ms 유지.
  재측정하지 않았다
- 검증: 전체 **673개 통과, 0개 건너뜀**(직전 650 통과·2 건너뜀). 신규 21개 —
  제공자 문의 17개, 의존성 선언 가드 4개
- 미수정 사전 결함: `python -m tools.validate`가 cp949 콘솔에서 em dash 출력 시
  `UnicodeEncodeError`로 중단된다. 작업 트리를 stash한 HEAD에서도 동일하게 재현되므로
  이번 변경과 무관하며, 범위를 넓히지 않기 위해 별도 작업으로 분리했다
- 남은 위험: 제출은 사용자 승인 사항이라 회신 전까지 촬영일은 미확인 상태로 남는다.
  질문 3(제공자 정확도 문서)이 열리지 않으면 촬영일을 확인해도 표식 분류는 계속 차단된다
- 다음 작업: 사용자가 문의를 제출하면 회신을 스키마에 기록하고 감사를 재실행한다.
  회신 대기 중에는 V2-2f와 독립적인 로드맵 항목으로 이동한다

### 2026-08-04 — V3-1a 외벽 모듈 실척·증거 감사

- 착수 근거: V2-2f가 제공자 회신 대기로 진행 불가라 §5 순서의 다음 독립 항목인
  V3-1로 이동. 사진 등록 가능성과 외벽 모듈 데이터셋이 대상
- 전수 조사: 렌더러가 실제로 쓰는 창 격자 치수는 `scene.frag`의 리터럴이다.
  9개 외벽 계열 각각 `bay_width` 3.2~5.8m, `floor_height` 3.05~4.8m이며
  **어느 값에도 출처 기록이 없었다**
- 정정된 전제: 처음에는 랜드마크 높이도 미출처로 보였으나 `ATTRIBUTION.md`에
  RSHP(318/246/101m), AS+GG(240m·30°/15° 패널), NAFI(동판 돔·24 기둥),
  visitseoul(63 Square), IFC Seoul이 URL과 함께 이미 기록돼 있었다. 결손은
  높이가 아니라 **모듈 치수**다. 확인 전에 등급을 내리지 않았다
- 사진 등록 가능 시점 **0개**: 보유 사진 중 픽셀 등록 가능한 카메라 위치·자세를
  공표한 것이 없다(CC0 모바일 사진은 EXIF만, 보도사진은 자세·원시자료 모두 없음).
  따라서 현재 자료로 모듈 치수를 사진측량으로 잴 수 없다
- 치수가 측정이 아님을 보이는 산술: 출처 있는 높이 ÷ 셰이더 층고 = 함의 층수는
  63 City **63.00**, FKI **50.00**로 정확히 정수이고, Parc.1 타워1/타워2/호텔은
  68.39/52.90/28.45로 정수가 아니다. 5개 중 2개가 건물 이름·반올림 층수에서
  역산된 값으로 보인다. 자기 일관성은 있으나 측정 근거는 아니다
- 감사 도구 `tools/audit_facade_modules.py`는 외벽 서술이 사는 세 곳(GLSL 분기,
  Python 스타일 상수, 빌드된 장면 자산)을 대조한다. 스타일 12종 전부 선언·처리
  확인, 미선언 0, 일반 격자 상속 0
- **개발 중 발견한 자체 결함**: 최초 판정은 "어딘가에 분기가 있으면 의도된 것"
  이었는데, 스타일 8은 창 격자 이후에도 분기가 하나 더 있어 격자 분기를 지워도
  검사를 통과했다. 테스트가 이 거짓 음성을 잡아내 판정을 **창 격자 이전의 실제
  `return`** 존재로 좁혔다. 좁힌 뒤 국회의사당 벽이 사무실 창 격자를 물려받는
  경로가 정상 차단됨을 확인
- 게이트: `module_dimensions_traceable=true`(모든 리터럴이 기록과 대응),
  `surveyed_facade_module_claim_allowed=false`(9개 계열 전부 등급 D),
  `photo_registration_available=false`
- 품질 변화: 없음. 런타임 형상 0건, 장면 정점 수정 0개. 셰이더·장면 자산 무수정.
  V3 추정치는 35%로 유지한다 — 증거 기반을 만들었을 뿐 화면은 바뀌지 않았다
- 성능: 렌더 경로 무변경으로 직전 frame/visual/physics p95 13.475/10.696/4.099ms
  유지. 재측정하지 않았다
- 검증: 전체 **686개 통과, 0개 건너뜀**(직전 673). 신규 13개 — 파서 정확도,
  스타일 전수, 돔이 창 격자에 도달하지 않음, 치수 드리프트·분기 소실·미기록
  출처·미지 등급 차단
- 남은 위험: 벽 면적이 가장 넓은 주거(22,548 벽 정점)·기관(7,902) 계열도 등급 D라
  화면에서 가장 넓은 면이 가장 근거가 약하다. 등급 D는 "틀렸다"가 아니라
  "확인되지 않았다"는 뜻이다
- 결손 자료: 랜드마크별 공표 층수, 커튼월 모듈·기준 베이 치수, IFC 타워의 공표
  높이, 주거·기관 계열의 층고, 자세가 풀리는 사진 또는 건축 입면도
- 다음 작업: V3-1b에서 공표 층수·입면 자료의 실제 확보 가능성을 조사해 등급 D
  치수 중 어느 것을 B로 올릴 수 있는지 판정한다. V2-2f는 회신 도착 시 재개한다

### 2026-08-04 — V3-1b 도입 층고와 도색 층고의 불일치 정량화

- V3-1a에서 빠뜨린 리터럴을 닫았다. 창 격자보다 더 중요한 값이 `simulator/scene.py`에
  따로 있다. `height` 태그가 없는 건물은 `building:levels × 3.2m`로, 두 태그가 모두
  없으면 `12.0m`로 높이를 얻는다. 이 값들은 표면 무늬가 아니라 **정점 위치**를 정한다
- 불일치 발견: 도입 층고 3.2m가 셰이더의 계열별 도색 층고(3.05~4.8m)와 다르다.
  따라서 `building:levels`로 높이를 얻은 건물은 화면의 층 띠 개수가 그 높이를 만든
  층수와 어긋난다. FKI −33.3%, Parc.1 −31.2%, IFC −30.0%, 63 −20.0%, 기관 −15.8%,
  국회 −11.1%, 호텔 −9.9%, 주거 +4.9%, 일반 −1.5%
- **과장하지 않기 위한 단서**: −33.3%를 장면 전체 오차로 읽으면 안 된다. 랜드마크
  계열은 대부분 명시적 높이를 받아 이 경로를 타지 않는다. 실제 영향은 태그 없는
  건물이 몰린 generic(−1.5%)·residential(+4.9%)에 집중되고 그쪽 불일치는 작다
- **고치지 않은 이유**: 둘을 맞추려면 태그 없는 모든 건물의 매스를 바꾸거나 모든
  계열의 층 띠를 바꿔야 하는데, 어느 값이 옳은지 말해 주는 근거가 없다. 런타임을
  조용히 바꾸지 않고 보고만 한다
- **다음 병목 규명**: 어느 건물이 어느 높이 경로를 탔는지 현재 확인할 수 없다.
  도로는 날짜가 박힌 OSM 스냅샷(`yeouido_road_osm_2024-10-05.json`)으로 보존돼
  있지만 건물은 가져오기 시점에 내려받아 태그를 버린다. 이 비대칭이 V3-1b의 원래
  질문(공표 층수로 등급 D를 B로 올릴 수 있는가)을 막는 실제 병목이다
- 품질 변화: 없음. 런타임 형상 0건, 장면 정점 수정 0개. 셰이더·장면 자산 무수정
- 성능: 렌더 경로 무변경으로 직전 frame/visual/physics p95 13.475/10.696/4.099ms 유지
- 검증: 전체 **689개 통과, 0개 건너뜀**(직전 686). 신규 3개 — 도입 층고 파싱·기록
  일치, 불일치 보고, 그리고 모든 도색 층고를 3.2m로 맞추면 불일치가 0이 되는지
  확인해 지표가 항상 거짓인 게 아님을 실증
- 남은 위험: 불일치를 실제 영향받는 건물에 귀속시키지 못한 상태다. 건물 태그가
  없으면 층수 증거도 없어 등급 D 치수를 B로 올릴 수 없다
- 다음 작업: 건물 OSM 스냅샷을 도로와 같은 방식으로 날짜 고정 보존한다. Overpass에
  대한 외부 조회이므로 사용자 승인을 받고 진행한다. 확보되면 `building:levels`로
  층수 증거를 만들고 높이 경로별 건물 수를 집계해 불일치의 실제 범위를 확정한다

### 2026-08-04 — V3-1c 건물 태그 스냅샷 보존과 높이 경로 귀속

- 사용자 승인을 받아 Overpass에서 **날짜 고정(2024-10-05T10:20:00Z) 건물 태그
  스냅샷**을 1회 조회해 `assets/yeouido_building_osm_2024-10-05.json`으로 보존했다.
  도로 스냅샷과 같은 도구·같은 패턴이며, ODbL 1.0과 질의문·엔드포인트를 함께 기록
- 기하는 의도적으로 제외했다(`out tags`). 이 보관물의 목적은 높이 출처 증거이지
  기하 재생성이 아니며, 장면 빌드는 자체 조회를 쓴다. 덕분에 1,314 way가 148KB로
  도로 스냅샷(887KB)보다 작다
- **높이 경로 귀속**: `height` 태그 652개(49.6%), `building:levels × 3.2m` 206개
  (15.7%), 태그 없음 → 12.0m 456개(34.7%)
- **직전 턴의 −33.3%는 장면의 어떤 건물도 설명하지 않는다.** 불일치에 노출된 건물은
  levels 경로 206개뿐인데 최악인 FKI·Parc.1 계열은 이 경로 건물이 **0개**다.
  206개 중 **202개(98.1%)**가 오차 5% 이하 계열(주거 172개 +4.9%, 일반 30개 −1.5%)에
  있고, 나머지는 glass-blue 2개(−30.0%)·국회 1개(−11.1%)·호텔 1개(−9.9%)다.
  귀속 자료 없이 최악값을 대표값으로 보고하지 않은 판단이 수치로 확인됐다
- **더 큰 발견**: 1,314개 중 456개(34.7%)가 `height`도 `building:levels`도 없어
  매스가 오직 12.0m 상수 하나로 정해진다. 출처 없는 상수 하나가 층고 불일치보다
  더 많은 건물의 매스를 결정하고 있다
- 드리프트 차단: 감사의 경로 분류가 `simulator.scene._height`의 분기 순서를 다시
  쓴 것이므로, 1,314개 건물 전수에 대해 분류 결과와 `_height` 실제 반환값이 일치하는지
  검사하는 테스트를 넣었다. 계열 판정은 `_facade_style`을 직접 임포트해 중복을 없앴다
- 스냅샷 없이도 감사가 동작하도록 분리했다(귀속만 `null`). 다른 체크아웃에서
  하드 의존이 되지 않는다
- 품질 변화: 없음. 런타임 형상 0건, 장면 정점 수정 0개. 셰이더·장면 자산 무수정
- 성능: 렌더 경로 무변경으로 직전 frame/visual/physics p95 13.475/10.696/4.099ms 유지
- 검증: 전체 **694개 통과, 0개 건너뜀**(직전 689). 신규 5개 — 경로 분류와 `_height`
  전수 일치, 노출 가중, 12.0m 기본값 비중, 스냅샷 날짜·기하 부재, 스냅샷 부재 내성
- 남은 위험: OSM은 커뮤니티 자료라 등급 상한이 B이고, 확보한 `building:levels`는
  랜드마크 타워를 하나도 포함하지 않는다. 등급 D 모듈 치수를 B로 올리려면 여전히
  공표 층수나 입면 자료가 필요하다
- 다음 작업: 12.0m 기본값이 실제로 어느 건물에 얼마나 걸리는지 화면 영향으로 환산하고,
  건축물대장(지상층수·높이) 경로로 등급 A 층수를 확보할 수 있는지 판정한다

### 2026-08-04 — V3-1d 12.0m 기본값의 타당성 실측

- 형상에서 확인: 이론이 아니라 실제 장면에서 건물 정점 **17,064개(16.4%)가 정확히
  Y=12.00m**에 있다. 0을 제외한 최빈 높이이고 2위(40.00m, 5,424개)의 3배가 넘는다.
  기본값 하나가 화면 건물 형상의 6분의 1을 직접 정하고 있다
- 이웃과 비교: 같은 날짜 스냅샷에서 높이가 있는 858개의 중앙값은 **40.0m**
  (사분위 20.0/40.0/57.6)이고, 12.0m 이하는 **15.4%**뿐이다
- 종류별 격차(표본 20개 이상만): apartments 태그없음 121개 vs 태그 중앙값 48.0m
  (**4.0배**), yes 163개 vs 24.0m(2.0배), commercial 119개 vs 20.0m(1.67배),
  office 1개 vs 40.0m(3.33배). 여의도 아파트 121개가 12m(약 4층)로 서 있는 상태다
- **편향을 명시**: 크고 눈에 띄는 건물일수록 높이 태그가 붙었을 가능성이 높다.
  그래서 전체 중앙값보다 종류별 비교를 신뢰하고, 결과를 **후보**로만 기록한다.
  표본이 20개 미만인 종류(house, construction, roof 등)는 후보를 내지 않는다
- **적용하지 않음**: 기본값을 바꾸면 태그 없는 456개 건물의 매스가 전부 움직이고,
  `yeouido_scene.npz` 재생성에는 건물 기하 재다운로드가 필요하다. 게이트
  `untagged_default_height_supported_by_evidence=false`로 고정하고 후보만 남겼다
- 품질 변화: 없음. 런타임 형상 0건, 장면 정점 수정 0개
- 성능: 렌더 경로 무변경으로 직전 frame/visual/physics p95 13.475/10.696/4.099ms 유지
- 검증: 전체 **697개 통과, 0개 건너뜀**(직전 694). 신규 3개 — 12.0m가 장면의 최빈
  비영 높이이고 2위의 3배 이상임을 감사와 독립적으로 확인, 이웃 대비 수치, 표본
  하한과 미적용 게이트
- 남은 위험: 이 후보는 OSM 자체 통계라 등급 상한이 C(공인 통계 모형에 의한 추정)다.
  등급 A는 건축물대장(지상층수·높이)에서만 나오며 아직 조사하지 않았다
- 다음 작업: 두 갈래다. (1) 장면 재빌드를 승인받아 종류별 기본값을 적용하고 전후
  스카이라인을 측정한다. (2) 건축물대장 경로로 등급 A 층수·높이를 확보할 수 있는지
  판정한다. (2)가 되면 (1)의 근거가 C에서 A로 올라간다

### 2026-08-04 — V3-1e 건축물대장 등급 A 경로 검증

- 포털 페이지에서 직접 확인했다(추정 아님). **국토교통부_건축HUB_건축물대장정보
  서비스**, Base URL `apis.data.go.kr/1613000/BldRgstHubService`, 엔드포인트
  `/getBrTitleInfo`. 응답 모델에 **`heit`(높이)·`grndFlrCnt`(지상층수)**가 있다
- `heit / grndFlrCnt`는 공식값 두 개에서 나온 층고다. 이것이 지금 9개 외벽 계열이
  가정하고 있는 값이며 12.0m 기본값이 대신하고 있는 값이다. 등급 D를 벗어나는
  유일한 경로다
- **협의가 필요 없다**: 무료 · 이용허락범위 제한 없음 · 개발/운영 모두 **자동승인** ·
  개발계정 10,000건/일. V1-11 DEM 신청이나 V2-2f S-Map 문의와 달리 심의가 자동이고
  남은 관문은 서비스 키 발급뿐이다. 키는 자격증명이라 저장소에 넣지 않는다
- **전수 조회 가능**: `bun`·`ji`가 선택 파라미터라 시군구+법정동 코드만으로 해당
  법정동 표제부 전체를 받을 수 있다. 법정동코드 **1156011000 서울특별시 영등포구
  여의도동**을 행정표준코드관리시스템에서 확인했고, API 분해값은 `sigunguCd=11560`,
  `bjdongCd=11000`이다. 다만 5+5 분해는 파생값이므로 첫 응답 전까지 검증된 값으로
  취급하지 않는다고 명시했다
- **건물별 결합은 약함**: 레지스터는 지번 기준인데 OSM은 도로명 계열이다. 높이 태그가
  없는 456개 중 `addr:housenumber` 보유 17개, `addr:*` 하나라도 보유 20개, `name`
  보유 152개. 따라서 첫 용도는 건물별 귀속이 아니라 모집단 통계다 — 대체하려는 값
  자체가 통계적 대역값이기 때문이다. 공간 결합이 필요하면 `GIS건물통합정보(WMS/WFS)`가
  더 나은 후보다(미조사)
- 자료를 지어내지 않았다: 응답이 기록되기 전까지 감사는 경로만 `ready`로 보고하고
  파생은 전부 차단한다. 표본 20개 미만이거나 파라미터 분해가 미확인이면 여전히 차단된다
- 자격증명 가드: 요청 패키지에 서비스 키처럼 보이는 값이 있으면 감사가 실패한다.
  일반 문장에는 반응하지 않음을 테스트로 확인했다
- 품질 변화: 없음. 런타임 형상 0건, 장면 정점 수정 0개
- 성능: 렌더 경로 무변경으로 직전 frame/visual/physics p95 13.475/10.696/4.099ms 유지
- 검증: 전체 **707개 통과, 0개 건너뜀**(직전 697). 신규 10개 — 경로·코드 정합,
  결합 평가가 스냅샷 실측과 일치, 자격증명 가드 양방향, 비현실적 쌍 배제,
  충분한 응답이면 게이트가 실제로 열림(공허하지 않음)
- 남은 위험: 5+5 코드 분해가 미확인이고, 대장 1건이 OSM way 1개와 1:1이 아니다
  (대장은 동 단위). 모집단 통계에는 문제가 적지만 건물별 적용에는 결합 문제가 남는다
- 다음 작업: 사용자가 data.go.kr에서 서비스 키를 발급하면 여의도동 표제부를 받아
  기록하고 감사를 재실행한다. 그 전까지는 V3-1d의 등급 C 후보 적용(장면 재빌드 승인)이
  독립적으로 진행 가능한 유일한 경로다

### 2026-08-04 — V0-2 전후 변화 기술 지표

- 착수 근거: V3-1의 두 갈래(서비스 키 발급, 장면 재빌드)가 모두 사용자 행동 대기라
  §5 순서에서 독립적으로 진행 가능한 V0 잔여 항목으로 이동했다. 이 지표는 나중에
  12.0m 기본값을 적용했을 때 **스카이라인이 어떻게 변했는지 measured로 말하기 위한
  전제**이기도 하다
- 기존 비교의 한계: `capture_visual_baselines --compare`는 "한도 이상 변했는가"만
  답한다. 회귀 게이트로는 그것이 정확히 필요한 전부지만, **무엇이** 변했는지는 말하지
  못한다. 스카이라인 이동, 엣지 흐려짐, 계조 변화가 전부 "변경 픽셀"로 뭉뚱그려진다
- 네 지표를 분리 구현했다(`simulator/validation/frame_comparison.py`)
  - **실루엣 IoU**: 깊이 버퍼 커버리지 마스크 기준. IoU만으로는 늘어난 것과 줄어든
    것이 같은 점수라서 added/removed 비율을 함께 보고한다
  - **엣지 변위**: Sobel 엣지의 대칭 Chamfer 거리, 단위는 픽셀. 단방향으로 재면
    후보가 엣지를 전부 잃었을 때 0으로 나오므로 대칭으로 측정한다
  - **계조 이동**: 휘도 분포의 Wasserstein-1 거리(코드값). 밝아진 픽셀과 어두워진
    픽셀이 상쇄돼도 평균차와 달리 사라지지 않는다
  - **시간 안정성**: 카메라 고정 상태의 연속 프레임 차이. 렌더러가 스스로와
    불일치하는 정도이며 반짝임 픽셀 비율을 함께 낸다
- **테스트가 잡은 자체 결함**: 후보가 엣지를 전부 잃은 경우, 대응 없는 거리는
  `inf`가 되는데 내 요약이 비유한값을 걸러내면서 **변위 0.0(완전 일치)으로 보고**했다.
  프레임 대각선으로 포화시키고 `unmatched_edge_fraction`을 함께 내도록 고쳤다
- 값은 전부 **기술적(descriptive)**이며 합격/불합격 임계값을 두지 않았다. 적절한
  임계값은 그 변경이 무엇을 의도했는지에 달려 있고, 여기서 임의로 정하면 설명이
  판정으로 바뀐다. 판정은 기존 회귀 게이트가 계속 맡는다
- 도구 `tools/report_visual_change.py`는 두 캡처 디렉터리를 읽어 뷰별 보고서를 낸다.
  한쪽에만 있는 뷰는 조용히 건너뛰지 않고 이름으로 보고하고, 해상도가 바뀐 뷰는
  비교 불가로 표시한다
- 품질 변화: 없음. 런타임 형상 0건, 렌더 경로 무수정. 오프라인 계측만 추가
- 성능: 렌더 경로 무변경. 지표 자체는 64×64 기준 호출당 약 5ms로 측정됐다
- 검증: 전체 **723개 통과, 0개 건너뜀**(직전 707). 신규 16개 — 전부 구성으로 정답을
  아는 프레임 쌍에 대해 검사한다(오프셋 사각형의 IoU, 3px 이동한 엣지, 40 코드값
  균일 이동, 상쇄되는 계조 변화, 정지/점멸 시퀀스). 0이어야 하는 지표에는 0이 아닌
  짝 사례를 붙여 "항상 0"이 "차이 없음"으로 읽히지 않게 했다
- **측정 신뢰성 경고**: 이번 전체 실행은 689초가 걸렸는데 직전은 46초였다. 변경하지
  않은 `tests/test_facade_modules.py`가 0.90초에서 6.28초로 7배 느려진 것으로 보아
  장비 상태 저하이며 이번 변경 탓이 아니다. 신규 테스트 파일 단독 비용은 약 15초로
  측정됐다(scipy.ndimage 최초 임포트 약 7초 포함). 벽시계 시간은 실행 간 비교 불가다
- 남은 위험: 실루엣 IoU는 깊이 커버리지 마스크를 필요로 하는데 캡처 경로가 아직
  마스크를 저장하지 않는다. 마스크가 없으면 추측하지 않고 `null`로 비운다 —
  표시 프레임에서 하늘을 분할하면 계조로부터 형상 경계를 지어내게 되고, 이 지표들이
  구분하려는 바로 그 두 가지가 섞인다
- 다음 작업: 캡처 경로에 깊이 커버리지 마스크 저장을 추가해 실루엣 IoU를 실제
  대표 시점에 연결한다. 그러면 12.0m 기본값 적용 시 스카이라인 변화를 IoU로 직접
  말할 수 있다

### 2026-08-04 — V0-3 깊이 커버리지 마스크 캡처 연결

- V0-2에서 실루엣 IoU는 커버리지 마스크를 요구하는데 캡처 경로가 마스크를 저장하지
  않아 항상 `null`이었다. 이번에 그 연결을 끝냈다
- `Renderer.scene_depth_texture`를 노출하고 `read_scene_coverage()`가 장면 깊이
  타깃을 읽어 기하 대 하늘 불리언 마스크를 만든다. 깊이는 원거리 평면으로 클리어되므로
  그보다 가까운 프래그먼트가 기하다
- **표시 프레임에서 하늘을 분할하지 않는 이유**를 코드에 남겼다. 그렇게 하면 노출
  변화가 형상 변화로 위장할 수 있고, 실루엣 지표는 바로 그 두 가지를 갈라놓으려고
  존재한다. 마스크가 없으면 추측하지 않고 `null`로 둔다
- 캡처 도구가 뷰마다 `{view_id}.coverage.png`(1비트 무손실)를 저장하고 체크섬·통계를
  보고서에 기록한다. 변화 보고 도구는 마스크가 있으면 실루엣을 채우고, 마스크가 없는
  이전 캡처에서도 나머지 지표로 계속 동작한다
- **실제 GPU 확인**: 대표 시점 `terrain_shoreline`을 실제 캡처했다. 1280×720,
  커버리지 0.6733. 진짜 실루엣임을 구조로 검증했다 — 하단 1/3 기하 100%, 중앙 78%,
  상단 1/3 24%, 최상단 10% 7%, 열별 지평선 행 중앙값 309·최대 343·최소 0
  (0은 화면 꼭대기까지 닿는 고층 건물)
- **실제 캡처에 스카이라인 12행 상승을 구성해 넣고 보고서를 돌렸다**: 실루엣 IoU
  0.9746, 커버리지 0.6733 → 0.6908, 추가 1.75%(16,169 px), 제거 0.0%, 엣지 평균
  0.25 px·p95 0.00 px, 계조 0.307 코드값. **기하는 늘었고 엣지는 그대로고 계조는
  안 변했다**는 세 갈래 진술이 일관되게 나온다. 기존 "변경 픽셀 수"로는 얻을 수
  없던 구분이며, 12.0m 기본값을 적용할 때 필요한 바로 그 서술이다
- 품질 변화: 없음. 런타임 형상 0건. 렌더 경로는 깊이 타깃 읽기 속성 추가 외 무수정
- 성능: 렌더 경로 무변경. 마스크는 1비트 PNG라 뷰당 수 KB 수준이다
- 검증: 전체 **729개 통과, 0개 건너뜀**(직전 723). 신규 6개 — GL 행 순서 뒤집힘
  교정(마스크가 상하 반전돼도 그럴듯해 보이므로 명시 검사), 잘린 깊이 버퍼 거부,
  깊이 타깃 없는 렌더러, 마스크 무손실 왕복, 보고서의 마스크 사용, 마스크 없는
  이전 캡처 내성. `capture_view` 반환값과 `write_capture` 시그니처를 바꿨으므로
  기존 `tests/test_visual_regression_views.py` 5개도 함께 확인했다
- **직전 턴 측정 경고의 사후 확인**: 이번 전체 실행은 97초다. 직전 689초는 장비 상태
  저하였고 이번 변경과 무관했다는 판단이 회복으로 확인됐다
- 다음 작업: V3-1의 두 갈래(서비스 키 발급, 장면 재빌드) 중 하나가 열리면 이 지표로
  전후를 측정한다. 그 전까지는 V0 완료 게이트의 마지막 항목인 실제 사진 3시점 등록
  가능 여부 판정을 건물 범위 밖으로 확장한다

### 2026-08-04 — V0-4 사진 등록 가능 여부 판정

- **전제를 바로잡았다**: 외관 참조 자료에는 "보유 사진 중 픽셀 등록이 가능한 자세를
  공표한 것이 없다"고 적혀 있는데, 이는 보이는 것보다 약한 진술이다. 자세는 공표될
  필요가 없고 **풀리면** 된다. `fit_camera_pose`가 기준점으로 자세를 푼다. 따라서
  진짜 질문은 "풀 대상이 있는가"다
- 필요 조건을 셋으로 분해했다: 내부표정(초점거리·센서), 측정 가능한 픽셀,
  그리고 좌표를 아는 식별 가능 지형지물 **최소 6점**(솔버 자체 요구값)
- 보유 사진 5건 판정: CC0 휴대폰 촬영 2건은 초점거리 4.65mm·35mm 환산 25mm와 픽셀
  보유가 모두 되어 **카메라 측면은 통과**한다. 언론사 3건은 카메라 메타데이터가 없고
  재배포 불가라 픽셀도 보유하지 않아 이중으로 막힌다
- **판정: 등록 가능 시점 0개.** 병목은 카메라가 아니라 **기준점**이다. 카메라 측면이
  통과한 2건조차 등록할 수 없다
  - NGII 공개 기준점 4개: 전부 `망실`이라 좌표와 무관하게 영상에서 식별 불가 → 0점
  - 서강대교 공표 제원: 등급 A이지만 원 도면이 프로젝트 EUS 좌표계로 등록돼 있지
    않아 이 좌표계에서의 기준점을 주지 못한다 → 0점
  - OSM 건물 모서리: 장면에서 가장 널리 식별 가능하지만 이 프로젝트 자신의 V-05
    근거상 평면 정확도 1~3m라 1m 목표를 밑돈다
  - 이 위에 세운 등록은 그 오차를 그대로 물려받으므로 **정확도를 진술할 수 없다**
- 두 번째 독립 사유: 기준점이 해결되어도 카메라 측면 통과가 2건뿐이라 V0가 요구하는
  3시점에 미달한다. 테스트로 실증했다(기준점을 충분하다고 주면 2건이 되고, 여전히
  3 미만)
- 솔버 요구값을 `MINIMUM_CONTROL_POINTS` 상수로 승격해 감사가 자체 숫자를 다시 쓰지
  않고 솔버 것을 인용하게 했다. 상수가 장식이 되지 않도록 5점은 거부되고 6점은
  받아들여지는 것을 실제 솔버 호출로 양방향 확인했다
- 기존 테스트 보존: 상수 도입 시 오류 문구를 숫자로 바꿨더니
  `tests/test_photogrammetry.py`의 기존 단언(`"at least six"`)이 깨졌다. 단언을 약화하지
  않고 **문구를 원래대로 되돌려** 기존 테스트를 그대로 통과시켰다
- 품질 변화: 없음. 런타임 형상 0건, 렌더 경로 무수정
- 성능: 렌더 경로 무변경으로 직전 frame/visual/physics p95 13.475/10.696/4.099ms 유지
- 검증: 전체 **736개 통과, 0개 건너뜀**(직전 729). 신규 7개
- 결손 자료: 야간 사진에서 식별 가능하면서 프로젝트 EUS 좌표계에서 1m 이하인
  지형지물 6점 이상, 서강대교 공표 제원의 EUS 등록(등급 A 제원을 실제 기준점으로
  바꿔 준다), 언론사 3건의 공표 내부표정(다만 기준점이 생기기 전에는 무의미)
- **V0 잔여 항목 2개가 모두 닫혔다.** 완료 게이트의 "한 명령 재현"과 "전후 비교
  보고서"는 V0-2·V0-3에서 갖췄고, 사진 등록 판정은 이번에 내렸다
- 다음 작업: V0 완료 게이트를 전체로 점검해 남은 조건이 있는지 확인하고, 없으면 V0를
  완료로 표시한다. V3-1의 두 갈래는 계속 사용자 행동 대기다

### 2026-08-04 — V0-5 완료 게이트 실측과 커버리지 회귀 연결

- V0 체크박스 7개가 모두 닫혔으므로 완료 게이트 자체를 실측했다. 선언이 아니라 측정으로
  판단하기 위해 대표 카메라 6종을 **독립적으로 두 번 캡처**해 비교했다
- **V0-3에서 내가 남긴 구멍을 먼저 메웠다**: 커버리지 마스크를 저장하고 매니페스트에
  체크섬까지 기록해 놓고도 `--compare` 회귀 게이트는 HDR·SDR만 비교하고 커버리지를
  보지 않았다. 마스크가 안 써지거나 비어 나와도 색상 비교는 멀쩡해 조용히 통과했을
  경로다. `compare_coverage()`를 추가해 게이트에 연결했고, 마스크가 없는 이전 캡처
  디렉터리는 색상만으로 비교해 만들 수 없었던 파일 때문에 실패하지 않게 했다
- **조건 1 실측 결과**: 메타데이터 불일치 0건. 6시점 전부 **SDR과 커버리지가 비트
  동일**(커버리지 변경 픽셀 0개). 선형 HDR은 5시점이 비트 동일이고
  `water_reflection` 1건만 8픽셀이 다르다
- 그 1건을 정밀 규명했다. 다른 픽셀은 **행 336~339, 열 598~599의 연속된 4×2 블록**
  하나이고 최대 절대오차는 1.204e-3으로 한도 1.0e-3을 20% 넘는다. 값은 선형 복사휘도
  7e-4 대 1.9e-3 수준의 거의 검은 픽셀이며 주변 명암비가 약 28배인 경계다
- 독립 4회 실행에서는 결과가 두 가지로만 갈렸다({a,d} / {b,c}). 이를 근거로 처음에는
  "이중안정"으로 판단했으나 **이 판단은 표본이 부족했고 V0-6에서 정정했다**. 위치와
  크기가 고정인 것은 맞다. 주 패스 실루엣은 비트 동일이므로 주 패스가 아니다
- **한도를 넓히지 않았다.** 1.0e-3 한도가 바로 이것을 잡아낸 장치다. 별도 작업으로
  분리했고, 표시 SDR 프레임은 비교를 통과하므로 톤매핑·8비트 양자화 뒤에는 남지
  않는다 — 영향은 선형 HDR 캡처에 한정된다
- **판정**: 조건 1은 표시 프레임과 실루엣에서 충족, 선형 HDR 비트 완전 재현은 미충족.
  조건 2(모든 시각 커밋이 전후 보고서 생성)는 도구는 갖췄으나 강제 장치가 없다.
  git hook이나 CI는 사용자 워크플로 설정이라 임의로 설치하지 않았다.
  **따라서 V0를 완료로 표시하지 않는다**
- 품질 변화: 없음. 런타임 형상 0건, 렌더 경로 무수정
- 성능: 렌더 경로 무변경으로 직전 frame/visual/physics p95 13.475/10.696/4.099ms 유지
- 검증: 전체 **738개 통과, 0개 건너뜀**(직전 736). 신규 2개 — 빈 마스크 검출,
  래스터 엣지 규모만 허용. 커버리지 허용 오차 1e-5는 실측으로 뒷받침된다:
  6시점 실제 변동이 0픽셀이었다
- 다음 작업: 반사 사전 패스 이중안정을 해소하면 조건 1이 완전히 충족된다. 조건 2의
  강제 장치는 사용자 결정 사항이다. V3-1의 두 갈래는 계속 사용자 행동 대기다

### 2026-08-04 — V0-6 반사 잔차 정정: 이중안정이 아니라 산발적

- **직전 판단을 정정한다.** V0-5에서 독립 4회 실행이 {a,d} / {b,c} 두 무리로 갈린 것을
  보고 "무작위가 아니라 이중안정"이라고 보고했다. 표본 4개로 내린 결론이었고, 표본을
  늘리니 틀렸다
- 한 프로세스 안에서 6회 연속 캡처한 결과: **5회가 서로 비트 동일이고 1회만 어긋난다.**
  어긋난 1회는 여전히 같은 4×2 블록(행 336~339, 열 598~599), 같은 최대오차 1.204e-3이다.
  4회 실행의 2대2 분할은 소표본이 만든 착시였다
- 따라서 정확한 성격은 **산발적 발생**이다. 관측 빈도는 대략 3~6회에 1회다. 이는 진단을
  바꾼다 — 깊이 동률 판정이라면 실행마다 결정적이어야 하는데, 산발적이라면 경합
  또는 미초기화·잔류 값 읽기 쪽이다
- 추가로 좁힌 사실: **한 프로세스 안에서도 발생하므로 프로세스별 초기화 차이가
  아니다.** 렌더 경로의 난수는 전부 명시 시드를 쓰는 것을 확인했으므로 미시드 생성기도
  아니다. 한 번은 같은 크기로 144픽셀이 어긋난 적이 있어 영향 범위도 고정이 아니다
- 위치 고정 + 크기 고정 + 산발적 발생의 조합은 반사 사전 패스의 특정 지점에서 일어나는
  경합 또는 stale read를 가리킨다. 공유 `airlight_fbo`가 같은 프레임에 미러 카메라용과
  실제 카메라용으로 두 번 쓰이는 지점이 후보다
- **추측으로 렌더 경로를 고치지 않았다.** 이 프로젝트 자신의 원칙대로, 원인을 짚지 못한
  상태에서 손대는 것은 트레이드오프이거나 손해다. 한도도 넓히지 않았다 — 1.0e-3
  한도가 이것을 잡아낸 장치다. 개선된 증거를 담아 별도 작업으로 넘겼다
- 품질 변화: 없음. 코드 변경 0건, 문서만 갱신
- 검증: 코드 변경이 없으므로 직전 738개 통과 상태가 그대로 유효하다. 이번 턴의 산출은
  측정과 정정이다
- 다음 작업: 반사 사전 패스 경합 해소(별도 작업), 또는 V3-1의 두 갈래가 열리기를 대기

### 2026-08-04 — V0-7 잔차의 단계·패스 국소화

- 방법론 교정이 먼저 필요했다. 처음 만든 프로브는 매 프레임 dt를 진행시켜서 수면이
  움직였고, 그 정상적인 변화가 찾으려는 비결정성을 덮어 버렸다. **dt=0으로 시간을
  얼리자** 남는 차이는 렌더러가 스스로와 불일치하는 것뿐이 되어 신호가 드러났다
- 중간 타깃을 전부 읽어 단계를 좁혔다. 19회 비교 기준으로
  **airlight 0건, ambient occlusion 0건, 주 패스 깊이 0건, 반사 깊이 0건**이 어긋났다.
  **두 깊이 타깃이 모두 비트 안정이므로 기하 래스터화는 완전히 결정적이다**
- 이는 V0-5의 "깊이 동률 판정" 가설을 **반증한다**. 깊이가 완전히 같은데 색만 다르다.
  즉 z-fighting이 아니라 **셰이딩** 단계의 문제다
- **잔차는 하나가 아니라 둘이었다.** 위치·크기·빈도가 서로 다르고 독립적으로 발생한다
  - **A(water 패스)**: 136~144px, 행 332~362, 열 525~536, 최대 7.63e-4.
    `water.draw`를 무력화하면 **완전히 사라진다**. 반사 색 텍스처 자체가 4px
    (행 183~191, 열 263~267, 최대 3.372e-3) 어긋나는 것이 확대돼 전달된다.
    반사 깊이는 비트 안정이므로 반사 기하가 아니라 반사 셰이딩이다
  - **B(scene 패스)**: 8px, 행 380~383, 열 598~599, 최대 **1.204e-3**.
    `scene.draw`를 무력화하면 **완전히 사라진다**. water·AO·haze·land·particles를
    각각 꺼도 살아남는다. **캡처 게이트의 1.0e-3 한도를 넘기는 것은 이 B다**
- 빈도는 시간을 얼린 상태에서 11회 비교 중 8회가 무언가 어긋나고, B 단독은 1~6회다
- 아직 규명하지 못한 것: scene 패스 안에서 깊이는 같은데 색이 갈리는 정확한 기전이다.
  동일 깊이의 공면 표면 한 쌍이 후보이며(`ATTRIBUTION.md`도 공면 중복 벽을 과거에
  다룬 적이 있다), 확인하려면 그 화면 좌표의 월드 위치와 삼각형 구성을 짚어야 한다
- 재현 도구를 저장소에 남겼다: `tools/probe_render_determinism.py`. 시간을 얼리고
  중간 타깃을 전부 비교하며 `--disable`로 주 패스를 하나씩 제거한다. 이 결과는 전부
  그 도구로 재현된다
- **여전히 추측으로 고치지 않았고 한도도 넓히지 않았다.** 기전을 짚기 전 수정은
  이 프로젝트 §3이 경계하는 손튜닝이다
- 품질 변화: 없음. 런타임 형상 0건, 렌더 경로 무수정(진단 도구와 타입 주석 정정만)
- 검증: 전체 **738개 통과, 0개 건너뜀**. 신규 테스트 없음 — 이번 산출물은 계측 도구와
  국소화 결과다
- 다음 작업: B의 기전 확정. 화면 (380~383, 598~599)에 해당하는 월드 위치를 역산해
  그 지점에 공면 표면이 겹쳐 있는지 확인한다

### 2026-08-04 — V0-8 B 기전 추적: 배제된 가설과 실패한 위치 특정

- 확정한 사실
  - B의 색 상태는 **정확히 2개**다. 16프레임을 얼려 반복해도 R 평균이
    **7.103086e-04**와 **1.879930e-03** 둘뿐이고 세 번째 값이 나오지 않는다
  - 영향 블록은 **쿼드 정렬**이다. 열 598~599(쿼드 1열), 행 380~383(쿼드 2행)
  - 그 블록의 깊이는 행 방향으로 **완전히 평탄**하다(|d/dx| = 0). 즉 수평 방향 프리미티브
    경계가 아니다. 행 간에는 약 1e-5씩 매끄럽게 변한다
  - 바로 아래 **행 385에 한 줄짜리 깊이 이상**이 있다(0.999150 대 이웃 0.999666/0.999647).
    한 행 두께의 더 가까운 기하가 끼어 있다. B와 인접하지만 같은 행은 아니다
- **배제한 가설: 이방성 필터·밉 선택.** `ScannedMaterialTextures`의 이방성을 8.0에서
  1.0으로 실제로 바꿔(지원 확인됨) 재측정했더니 상태가 여전히 2개이고 **두 값이
  바이트 단위로 동일**했다. 필터링 선택이 원인이면 값이 달라져야 한다. 아니다
  - 첫 시도는 텍스처를 찾지 못해 아무것도 바꾸지 못한 무효 실험이었다. 소유자
    (`renderer.scene.scanned_materials.texture`)를 짚어 다시 했다
- **정정: 위치 특정에 실패했다.** 깊이를 역투영해 블록이 EUS (92.3, 0.001, 48.3),
  즉 수면 기준면 Y≈0에 있다고 이번 턴 중간에 말했는데, 검증해 보니 성립하지 않는다.
  장면 자산의 어느 정점 배열도 그 지점 **76m 이내에 없다**(가장 가까운 road 99.3m,
  detail 104.1m). 패스 이분법은 B가 scene 패스 소유라고 말하는데 역투영은 scene
  기하가 없는 곳을 가리킨다. **둘이 모순되므로 역투영을 신뢰할 수 없고, 위치를
  주장하지 않는다.** 깊이 0.9997은 원거리 평면에 가까워 역투영 오차가 크게 증폭되며,
  투영 행렬 규약을 아직 확인하지 못했다
- **방법론 한계 기록**: "패스 X를 끄면 사라진다"는 X가 원인인 경우와, X가 없어져
  화면 구성이 바뀌면서 변동하던 표면이 더 이상 보이지 않게 된 경우를 구분하지 못한다.
  필요조건이지 충분조건이 아니다. V0-7의 국소화는 이 한계 안에서 읽어야 한다
- 여전히 추측 수정 없음, 한도 조정 없음. 저장소 코드 변경 0건(문서만)
- 검증: 코드 변경이 없으므로 직전 738개 통과가 그대로 유효하다
- 다음 작업: 투영 행렬 규약(깊이 범위, reversed-Z 여부)을 먼저 확인해 역투영을
  교정한다. 그다음 교정된 좌표로 다시 기하를 조회하고, 패스 이분법의 확증을 위해
  "끄기" 대신 각 패스를 **단독으로만 그리는** 방식으로 교차 검증한다

### 2026-08-04 — V0-8 B의 표면과 기전 확정

- **내 분석 버그를 먼저 잡았다.** 프로브는 HDR을 뒤집지 않고(GL 순서, 아래가 0행),
  임시 스크립트는 깊이를 `flipud`(이미지 순서)해 읽은 뒤 둘 다 "행 380~383"으로
  색인했다. 44행 어긋난 좌표를 본 것이다. GL 380~383행은 **이미지 336~339행**이고,
  이는 최초 캡처 비교가 보고한 바로 그 행이다
- 그래서 직전 턴의 "그 지점에 scene 기하가 없다 → 위치 특정 실패"는 **잘못된 좌표에서
  나온 결론**이었다. 좌표를 바로잡고 다시 조회했다
- 깊이 0.99997은 원거리 평면에 가까워 거리 성분이 불량조건이므로, 점 조회 대신
  **카메라에서 8개 픽셀 방향으로 광선을 쏴서** 교차 삼각형을 찾았다
- **결과: 8개 광선이 전부 같은 삼각형에 맞는다.** `building_vertices` 삼각형 10621,
  거리 **1,515.6m**, 수직 벽면(surface=0), **facade_style 3.0 = FACADE_RESIDENTIAL**,
  법선 (0.579, 0, −0.816). 즉 B는 **1.5km 밖 주거 건물 파사드**다
- 패스 귀속도 39회 비교로 확정했다. B 발생 빈도는 기본 8/39, water 끔 6/39,
  land 끔 2/39, **scene 끔 0/39**다. V0-7의 scene 귀속이 옳았고, 직전 턴에 그것을
  의심한 것은 과잉 교정이었다
- **기전(강한 정황, 미확정)**: `scene.frag`의 창 격자는 `grid = surface_uv /
  vec2(bay_width, floor_height)`에 대해 `aa = min(fwidth(grid) * 1.25, .12)`로
  안티에일리어싱하고, `occupied`와 `floor_blackout`을 **`step()`으로** 정한다.
  1,515m에서 픽셀당 약 1.1m인데 주거 계열 베이는 3.4m × 3.05m라 한 베이가 약 3픽셀,
  즉 **표본화 한계**다. 쿼드 도함수 `fwidth`가 실행마다 미세하게 달라지면 `step`의
  이산 판정이 뒤집혀 **정확히 두 개의 복사휘도 값**이 나온다. 관측된 두 값이 이방성
  설정과 무관하게 바이트 동일한 것도 이와 맞는다(격자는 텍스처가 아니라 절차적이다)
- 아직 확정하지 못한 것: `fwidth`의 입력(헬퍼 인보케이션 값)이 실제로 실행마다
  달라지는지의 직접 증거. 이것만 확인하면 기전이 닫힌다
- 도구 개선: `tools/probe_render_determinism.py`에 **행 순서 함정**을 명시했다.
  이 도구는 GL 행(아래부터), 캡처 도구는 이미지 행(위부터)을 보고하므로
  `height - 1 - row`로 환산해야 한다. 내가 빠진 함정이라 문서로 막았다
- 품질 변화: 없음. 런타임 형상 0건, 셰이더 무수정
- 검증: 저장소 변경은 도구 주석뿐이므로 직전 738개 통과가 그대로 유효하다
- 다음 작업: `fwidth` 입력 변동을 직접 확인해 기전을 닫고, 그다음 수정은 V2의
  "카메라 이동 중 반짝임과 반복 타일 패턴 제거" 항목과 함께 다룬다 — 원거리에서
  창 격자를 표본화 한계 아래로 수렴시키는 문제이기 때문이다

### 2026-08-04 — V0-9 fwidth 가설 반증과 남은 후보

- V0-8에서 제시한 "쿼드 도함수 `fwidth`가 흔들려 `step` 판정이 뒤집힌다"를 직접
  검산했고, **반증됐다**
- 광선 교차점에서 파사드 상의 픽셀 발자국을 실측했다: **픽셀 열당 1.787m,
  픽셀 행당 1.770m**. 주거 계열 베이는 3.4m × 3.05m이므로
  - 셀당 픽셀 수는 가로 **1.90**, 세로 **1.72** — 나이키스트(셀당 2)를 밑도는
    **표본화 한계 이하**다. 격자가 심하게 에일리어싱된다는 점은 확인됐다
  - 그런데 `aa = min(fwidth(grid) * 1.25, .12)`에서 `fwidth(grid)*1.25`는
    **0.657·0.725**로 클램프 0.12의 **5.5배·6.0배**다. 즉 `aa`는 두 상태 모두에서
    0.12에 **고정**되어 있다. 도함수가 실행마다 흔들려도 클램프가 흡수하므로
    `aa`는 변하지 않는다 — **`fwidth`는 변동 입력이 아니다**
- 대안으로 세운 "`cell = floor(grid)`가 정수 경계에서 1 ULP 차이로 뒤집힌다"도
  **반증됐다**. 해당 8픽셀의 `fract(grid.x)`는 **0.212와 0.737**, `fract(grid.y)`는
  0.105~0.944로, 경계에서 충분히 떨어져 있다. 1 ULP 수준의 보간 차이로는
  `floor()`가 뒤집히지 않는다
- 따라서 **기전은 아직 닫히지 않았다.** 두 가설을 제거해 탐색 공간을 좁힌 것이
  이번 턴의 성과다. 남은 후보는 셰이딩의 다른 입력 — 정적 조명 목록의 구성·누산
  순서, 또는 `reflected_radiance` 경로 — 이며, 관측된 두 값(7.103086e-04와
  1.879930e-03, 비 2.65)이 **작은 발광 기여의 유무**로 보이는 점과 맞춰 봐야 한다
- 확정 방법은 셰이더 계측이다. 중간량(`aa`, `cell`, `occupied`, `pane`, 조명 누산)을
  차례로 출력해 어느 것이 실행마다 달라지는지 보면 닫힌다. 출하 셰이더를 임시
  수정하는 작업이라 이번 턴에서는 실행하지 않았다
- **가치 있는 부수 확인**: 이 파사드는 1.5km에서 셀당 2픽셀 미만으로 표본화된다.
  기전과 무관하게 이것 자체가 V2의 "카메라 이동 중 반짝임" 항목이 다뤄야 할 조건이다
- 품질 변화: 없음. 코드 변경 0건(문서만)
- 검증: 코드 변경이 없으므로 직전 738개 통과가 그대로 유효하다
- 다음 작업: 셰이더 계측으로 변동 입력을 특정한다. 출하 셰이더를 임시로 고쳤다가
  되돌리는 작업이므로 진행 전에 사용자에게 알린다

### 2026-08-12 — V0-10 B 중간량 계측과 수정 후보 반증

- `tools/probe_render_determinism.py`에 출하 `scene.frag`를 파일에서 바꾸지 않고 **프로그램
  생성 중에만** 중간량 출력을 주입하는 계측 경로를 추가했다. 알려진 GL 영역
  `(598, 380)–(600, 384)`만 잘라 비교하고, 각 상태의 발생 횟수와 RGB 범위를 기록한다
- 직접 계측에서 좌표, `aa`, 셀, 점유, 창 마스크, 파사드 색, 반사율, 발광, 원시 법선,
  월드 위치, 시선 방향과 광원 개수는 고정돼 있었다. 최종 B와 같은 8픽셀·약
  `1.2e-3` 서명은 `reflected_radiance` 계열을 끝까지 실행할 때 다시 나타났다
- 한 지점만 떼어 출력하면 `n_dot_v`, grazing Fresnel, 환경 specular 또는 동적 광원 루프
  뒤에서 처음 변하는 것처럼 보였지만, **계측 위치나 함수 경계를 바꾸면 최초 변동 지점도
  이동했다.** 즉 이 상관을 특정 물리 항의 인과로 채택할 수 없다
- 다음 수정 후보를 실제로 적용해 긴 반복으로 반증했고 모두 되돌렸다
  - 명시적 법선 정규화: 최종 191회 비교 중 30회 변동
  - `dot(n, view)`의 성분별 순차 누산: 383회 중 52회 변동
  - 시선 코사인을 호출부에서 스칼라로 전달: 해당 중간량은 안정화됐지만 최종 383회 중
    49회 변동
  - 동적 광원 루프를 uniform 개수 직접 상한으로 변경: 루프 직후 계측은 안정화됐지만
    최종 511회 중 61회 변동
  - `fwidth(surface_uv)`를 분기 전에 계산: 최종 1,023회 중 193회 변동
- 반사 사전 패스를 매회 갱신하지 않고 재사용해도 191회 중 60회 변동해, 반사/본 패스의
  카메라 uniform 교체도 원인이 아니었다. 정적 광원 수를 0으로 고정해도 두 상태가 남았다
- 현재 환경은 **Intel Arc 140V, OpenGL 4.3, 드라이버 32.0.101.8626**이다. 중간량은
  수학적으로 고정인데 셰이더 소스의 작은 계측 변화에 따라 변동 위치가 이동하므로,
  현재 증거가 지지하는 가장 좁은 결론은 **이 드라이버의 셰이더 최적화 형태에 민감한
  비결정성**이다. 최소 재현 셰이더나 다른 드라이버/GPU 교차 검증 전에는 더 구체적인
  기전을 주장하지 않는다
- 품질 변화: 없음. 효과가 없던 런타임 셰이더 수정은 모두 되돌렸고, 출하 셰이더는
  V0-9와 동일하다
- 검증 산출물: `docs/validation/render_determinism_v0/README.md`와 재사용 가능한 중간량
  계측 도구. 도구 소스 변환의 실패 폐쇄 계약은 단위 테스트로 고정했다. 전체 테스트는
  **743개 통과, 0개 건너뜀**이다
- 다음 작업: 동일 프로브를 다른 GPU/드라이버에서 교차 실행하거나, `scene.frag`의
  파사드 경로를 최소 독립 셰이더로 축소해 Intel 드라이버 재현 여부를 판정한다. 그 전에는
  출하 화면을 바꾸는 우회 수정을 채택하지 않는다

### 2026-08-12 — V0-11 잔차 B 최소 독립 셰이더 재현 판정

- `tools/probe_facade_shader_reproducer.py`를 추가했다. 시뮬레이터·도시 메시·텍스처·깊이
  버퍼·다른 패스를 모두 제거하고 **2×4 RGBA16F** 타깃만 사용한다
- 입력은 알려진 GL 영역 `(598, 380)–(600, 384)`에서 V0-10 계측으로 확인한 범위로
  고정했다. 원시 법선, 월드 위치, 반사율, 파사드 선형 RGB와 `water_reflection` 카메라를
  사용하며 정적·동적 광원 수는 실제 반증 조건과 같이 0으로 둔다
- 원시 법선 → 시선 방향 → `n_dot_v` → grazing Fresnel → 환경 반사 인라인 → 환경 반사
  함수 → 0개 광원 루프를 포함한 전체 함수 → 발광을 더한 최종 파사드 순서로 계산을
  누적한다. 함수 경계와 uniform 개수 기반 루프도 별도 단계로 남겨 최적화 형태를 비교한다
- Intel Arc 140V, 드라이버 `32.0.101.8626`에서 단계당 **4,096회**, 총 **32,768회**를
  실행한 결과 8단계가 모두 단일 상태였고 다른 draw는 0회였다. 독립 2×4 셰이더는 잔차를
  재현하지 않았다
- 같은 실행 환경에서 실제 장면 프로브를 96회 다시 돌리면 95회 비교 중 **27회**에 걸쳐
  동일한 8픽셀·최대 `1.204e-3` HDR 차이가 재현됐다. 따라서 샘플 수 부족이나 드라이버
  변경으로 독립 재현이 사라진 것이 아니다
- 결론: 파사드 수학·고정 입력·함수 경계·0개 광원 루프만으로는 충분하지 않다. 남은 최소
  조건은 실제 메시 보간/래스터화, 깊이 부착 및 테스트, 큰 뷰포트의 픽셀 위치, 전체
  `scene.frag` 프로그램 상태 중 하나 이상이다
- 품질 변화: 없음. 출하 셰이더와 렌더 경로는 바꾸지 않았다
- 검증 산출물: 단계별 SHA-256·발생 횟수·GPU 정보를 담은
  `docs/validation/render_determinism_v0/standalone_intel_arc_140v.json`. 재현기 소스 생성과
  상태 집계 계약을 단위 테스트로 고정했다
- 다음 작업: **V0-12 실제 패스 상태 사다리**. 독립 재현기에 (1) 1280×720 뷰포트와 실제
  픽셀 좌표, (2) 실제 파사드 삼각형 보간, (3) 깊이 부착·테스트, (4) 전체 `scene.frag`
  uniform/프로그램을 순서대로 추가해 최초 재현 조건을 찾는다

### 2026-08-12 — V0-12 실제 패스 상태 사다리

- `tools/probe_facade_pass_ladder.py`를 추가했다. 원본 장면 메시를 실제 카메라로 CPU
  투영해 알려진 8픽셀을 덮는 최전면 파사드 쿼드를 자동으로 찾고, 소스가 바뀌면 실패
  폐쇄한다
- 대상은 건물 메시 삼각형 **34588–34589**, 정점 **103764–103769**다. 두 삼각형은 GL
  픽셀 중심 `x=598–599, y=380–383` 전체를 덮으며 원시 법선
  `(-0.981241, 0, -0.192785)`은 V0-10 계측과 일치한다
- 다음 상태를 순서대로 누적했다
  1. 1280×720 실제 뷰포트와 원래 `gl_FragCoord`
  2. 원본 두 삼각형의 월드 위치·법선 보간
  3. RGBA16F 색상과 깊이 부착·깊이 테스트
  4. 실제 `scene.vert`/`scene.frag`, 재질 uniform 표와 텍스처 상태
  5. 전체 프로그램과 깊이 부착·테스트의 결합
- Intel Arc 140V, 드라이버 `32.0.101.8626`에서 단계당 **1,024회**, 총 **5,120회**를
  실행한 결과 다섯 단계 모두 단일 상태였고 다른 draw는 0회였다. 깊이 유무에 따른 출력
  해시도 각각 동일했다
- 같은 환경에서 실제 전체 장면을 정상 종료까지 64회 실행하면 63회 비교 중 **50회**에
  기존 8픽셀·최대 `1.204e-3` HDR 차이가 재현됐다. 단일 쿼드 측정의 음성 결과가 잔차
  소실이나 낮은 표본 수 때문은 아니다
- 결론: 실제 픽셀 위치, 원본 보간, 깊이, 전체 셰이더 프로그램 중 어느 것도 단독 또는
  단일 쿼드 결합으로 충분하지 않다. 남은 차이는 대형 건물 배치의 정점 처리, 여러 VAO
  드로우 순서, 선행 land/sky 프로그램 전환, 또는 매 프레임 동일 uniform 재기록이다
- 품질 변화: 없음. 출하 셰이더·메시·렌더 경로는 바꾸지 않았다
- 검증 산출물: `docs/validation/render_determinism_v0/pass_ladder_intel_arc_140v.json`.
  단계별 SHA-256·발생 횟수·GPU 정보와 선택한 원본 삼각형의 화면 좌표를 기록했다
- 다음 작업: **V0-13 배치·상태 갱신 사다리**. 단일 쿼드에서 (1) 전체 건물 VAO,
  (2) 전체 `ScenePass.draw()`, (3) land→scene 선행 드로우, (4) 매 프레임 카메라·조명
  uniform 재기록을 순서대로 추가해 최초 재현 조건을 확정한다

### 2026-08-12 — V0-13 배치·상태 갱신 사다리

- `tools/probe_facade_batch_ladder.py`를 추가했다. V0-12와 같은 비공개 RGBA16F+깊이
  타깃 및 8픽셀 scissor에서 건물 VAO, Scene VAO 접두사 2–5개, 전체 ScenePass,
  land/sky 선행 드로우, 매 프레임 uniform 재기록을 누적 비교한다
- 건물 배치는 **115,446정점**이고 ScenePass의 주 VAO는 5개
  (`115446, 40464, 497700, 5469, 238680`정점)다. 알려진 파사드 쿼드 6정점은 동일한
  전체 `scene.vert`/`scene.frag`, 카메라, 텍스처, 깊이 조건을 계속 사용한다
- Intel Arc 140V, 드라이버 `32.0.101.8626`에서 단계당 **2,048회** 측정했다. V0-12의
  6정점 단일 드로우는 1,024회 안정적이었지만 전체 건물 VAO 단일 드로우는 독립 측정
  두 번에서 각각 **6회와 1회** 두 번째 상태가 나타났다
- 최신 기계 판독 보고서에서 소수 상태는 정확히 같은 8픽셀 전부를 바꾸며 격리된 장면
  출력의 최대 RGB 차이는 `1.572609e-3`이다. 우세/소수 상태 SHA-256은 각각
  `c85f6aac…` / `ec5e576a…`로 배치 접두사 단계에서도 동일하다. 전체 렌더의 후속 합성 뒤
  관측되는 B는 기존대로 최대 `1.204e-3`이다
- 따라서 **추가 Scene VAO, land/sky 드로우, uniform 재기록은 필요조건이 아니다.** 같은
  셰이더와 같은 대상 파사드라도 한 draw에서 처리하는 건물 배치 규모 또는 버퍼 내 위치가
  추가되면 Intel 드라이버의 두 상태가 재현된다. 후속 단계의 0회 표본은 낮고 세션별로
  달라지는 발생률 때문에 안정화 효과로 해석하지 않는다
- 품질 변화: 없음. 출하 배치, 셰이더, 렌더 순서는 바꾸지 않았다
- 검증 산출물: `docs/validation/render_determinism_v0/batch_ladder_intel_arc_140v.json`.
  각 단계의 상태 해시·횟수뿐 아니라 우세 상태 대비 픽셀 수·최대 RGB 차이·행/열 범위를
  기록한다. 상태 차이 계산 계약과 uniform 갱신 순서는 단위 테스트로 고정했다
- 다음 작업: **V0-14 건물 draw 임계조건 사다리**. (1) 전체 VBO에서 대상 6정점만 offset
  draw, (2) 대상까지의 정점 접두사, (3) 원본 건물 103,866정점, (4) 옥상 디테일 포함
  115,446정점을 비교하고 접두사 길이를 이진 탐색해 버퍼 크기·정점 offset·처리 정점 수 중
  실제 필요조건을 판정한다

### 2026-08-13 — V0-14 건물 draw 임계조건과 9정점 재현

> 2026-09-07 정정: V0-12~14의 측정 도구는 FBO 바인딩 전 설정한 scissor가
> 바인딩 시 전체 화면으로 복원되는 오류가 있었다. 기존 JSON은 **전체 화면 렌더 후
> 8픽셀 읽기**의 역사적 측정값으로 보존한다. 아래의 "8픽셀 scissor" 및 드라이버
> 원인 해석은 V0-15A의 교정 결과와 함께 읽어야 한다.

- `tools/probe_facade_draw_threshold.py`를 추가했다. ModernGL의 `first`/`vertices` 범위 draw를
  사용해 전체 건물 VBO의 대상 offset, 대상 앞 정점 수, 원본 건물 경계, 옥상 디테일 꼬리를
  삼각형 단위로 분리한다. 모든 측정은 V0-12와 같은 비공개 RGBA16F+깊이 타깃에서만 한다
- 단계당 4,096회 1차 측정에서 다음을 확인했다
  - 전체 115,446정점 VBO에서 대상 6정점만 offset draw: **0회**
  - 대상까지 103,770정점과 원본 건물 103,866정점: 각각 **0회**
  - 옥상 디테일까지 포함한 115,446정점: **2회**
  즉 버퍼 크기·대상 offset·대상 앞 처리량만으로는 충분하지 않다
- 옥상 꼬리를 단계당 8,192회 이분 탐색했다. 총 115,194정점까지는 모두 안정적이지만 바로
  다음 삼각형을 포함한 **115,197정점부터 59회** 변동했다. 최초 양성 삼각형은 월드 좌표
  약 `(2157, 13, -124)m`, 화면 약 `(1245, 375)`의 콘크리트 옥상 박스 측면으로 문제
  8픽셀 `(598–599, 380–383)`과 겹치지 않는다
- 같은 115,197정점 draw에서 마지막 삼각형만 직전 삼각형 데이터로 바꾸면 4,096회 중
  원본은 **9회**, 교체본은 **0회**였다. 반면 전체 배치에서 이 삼각형만 생략해도 4,096회
  중 1회 남았으므로 이 삼각형은 절대 필요조건이 아니라 강한 증폭 입력이다
- 건물 draw를 원본 경계나 트리거 직전에서 둘로 나눠도 단계당 8,192회에서 각각 13회와
  40회 변동해 **draw 분할은 우회책이 아니다**
- 최종 최소 대조는 해당 옥상 삼각형 3정점과 대상 파사드 6정점만 복사한 **9정점 VBO**다.
  대상 파사드만 그리면 8,192회 안정적이었고 두 형상을 함께 그리면 양 draw 순서에서 모두
  두 상태가 재현됐다. 순서별 빈도는 독립 실행 사이에 뒤집혀 특정 순서를 인과로 채택하지
  않는다
- 결론: V0-13의 "대형 배치 규모"는 필요조건이 아니었다. 현재 가장 작은 지지 가능한
  재현 조건은 **서로 화면상 비겹침인 특정 옥상 삼각형과 대상 파사드 쿼드를 동일 GL 상태에서
  함께 처리하는 것**이었다. 당시 드라이버 상태 오염을 의심했지만, V0-15A에서 측정
  범위 오류가 확인되어 원인 확정은 보류한다. 셰이더·텍스처 경로와 드라이버를 아직
  분리하지 못했다
- 품질 변화: 없음. 출하 VBO·셰이더·draw 순서는 변경하지 않았고, GPU 버퍼 교체 대조는
  검증 앱 안에서 즉시 복원했다
- 검증 산출물: `draw_threshold_intel_arc_140v.json`,
  `draw_threshold_rooftop_tail_intel_arc_140v.json`,
  `draw_threshold_final_boundary_intel_arc_140v.json`,
  `draw_threshold_replacement_intel_arc_140v.json`, `draw_threshold_split_intel_arc_140v.json`,
  `draw_threshold_trigger_intel_arc_140v.json`, `draw_threshold_compact_order_intel_arc_140v.json`
- 다음 작업: **V0-15 최소 9정점 셰이더 입력 축소**. 옥상 삼각형의 위치·법선·surface·UV를
  하나씩 대상 또는 중립 값으로 바꿔 증폭 입력을 특정하고, 전체 `scene.frag`를 V0-11의
  BRDF 부분집합으로 다시 축소해 Intel 드라이버에 제출 가능한 최소 재현을 만든다

### 2026-09-07 — V0-15A scissor 교정 및 입력·BRDF 축소 대조

- `Framebuffer.use()`가 해당 FBO의 viewport/scissor를 복원함을 로컬 GL 실행으로
  확인했다. 기존 `_measure_draws()`는 이전 FBO에 scissor를 설정해 실제로는 전체 화면을
  렌더했다. 이제 측정 FBO에 직접 설정하고 반복마다 유효 범위를 검사하며 종료 시 이전
  FBO와 viewport/scissor를 복원한다. GPU occlusion query 회귀 테스트로 전체 화면
  삼각형의 실제 통과 샘플이 제한군 **8개**, 대조군 **921,600개**임을 확인했다
- `tools/probe_facade_input_reduction.py`는 같은 9정점에서 트리거의 법선, surface, UV,
  위치(퇴화 삼각형)를 하나씩 바꾼다. 대상 단독, 양 draw 순서, 측정 전후 동일 원본도
  포함한다. 원본 프로그램/환경 BRDF 부분집합 × 전체 화면/2×4 제한 × 8조건으로
  **32단계, 단계당 1,024회, 총 32,768회** 측정했다
- GPU/드라이버는 기존과 같은 Intel Arc 140V / `32.0.101.8626`이다. 전체 화면의 원본
  프로그램에서 법선 교체·UV 영점·마지막 원본 대조는 각각 **61·53·57회** 우세 상태와
  달랐다. 두 상태 해시는 기존 `c85f6aac…` / `ec5e576a…`와 같고 최대 RGB 차이는
  `1.572609e-3`이다
- **시간상 일정한 출력도 대상 단독과 다를 수 있다.** 전체 화면의 첫 원본 대조와
  target→trigger 순서는 1,024회 모두 `ec5e576a…`, 대상 단독은 모두 `c85f6aac…`였다.
  보고서에 시간 변동 횟수와 대상 단독 대조군 밖 상태의 샘플 수를 별도로 기록한다
- 원본 프로그램의 실제 2×4 제한군은 8조건 모두 `c85f6aac…` 단일 상태였다. 전체 화면의
  surface=대상 값(0), 위치 퇴화 조건도 같은 상태였다. 법선·UV 변경 후에도 변동이 남아
  그 원래 값은 이번 재현에 필수적이지 않았다. surface/래스터 범위에 민감하다는 관측이며,
  유한·순차 표본만으로 필요조건이나 해결책을 확정하지 않는다
- 환경 BRDF 부분집합은 16조건 모두 같은 단일 상태였다. 생산 vertex 계산·uniform·지형
  샘플링은 유지했지만 재질은 V0-11의 합성 입력이므로 생산 출력과 수치 동등하지 않다.
  **축소한 fragment shader에서는 아직 양성 재현이 없으며 독립 제출용 재현은 미완료**다
- 품질 변화: 없음. 생산 셰이더·장면 데이터·렌더 경로는 변경하지 않았다. scissor 수정은
  진단 도구에만 적용했다. 기존 V0-12~14 JSON을 덮어쓰지 않았다
- 산출물: `docs/validation/render_determinism_v0/input_reduction_intel_arc_140v.json`.
  정확한 9정점, 입력/셰이더 해시, 유효 scissor, GL 오류, 단계별 상태와 대조 비교를 포함하며
  각 단계가 끝날 때 저장한다. `complete: false`는 중단된 부분 결과를 뜻한다
- 검증: 전체 pytest **773 passed, 3 skipped**. 이후 대조군 비교 테스트를 추가하고 관련
  파일을 재실행해 **13 passed**를 확인했다. 모든 GPU 측정 단계에서 `GL_NO_ERROR`였다
- 다음 작업: **V0-15B 양성 fragment 경로 축소**. 전체 화면 원본/대상 단독 대조를 유지하고
  트리거의 surface 12 콘크리트 경로, 스캔 재질 샘플링·법선 변형 등을 단계적으로 축소한다.
  양성 재현을 유지하는 부분집합을 찾은 뒤 앱 초기화·텍스처 의존성을 분리한다. 드라이버
  원인 확정 및 외부 제출은 아직 하지 않는다

### 2026-09-07 — V0-15B 양성 fragment 경로 축소

- `tools/probe_facade_fragment_reduction.py`를 추가했다. 9정점과 생산 vertex shader,
  uniform·텍스처는 유지하고 비파사드 fragment 경로만 바꾼다. 파사드 본문은 문자열
  그대로 보존하며, 축소 지점이 달라지면 실패하도록 검사한다. 각 프로그램에서 대상 단독
  → 트리거→대상 → 대상→트리거 → 대상 단독의 4조건을 비교한다
- 기존 프로그램과 같은 소스를 새로 컴파일한 대조를 포함한다. 매 실행의 처음·끝에는
  원래 프로그램을 재사용한다. 프로그램별 대상 단독 앞·뒤 상태가 일치하지 않으면
  `trigger_effect_observed`를 `null`로 남겨 트리거 효과를 판정하지 않는다
- 같은 Intel Arc 140V / `32.0.101.8626`에서 3회 실행, **84단계 × 1,024회 = 86,016회**
  측정했다. 모든 대상 단독 대조는 `c85f6aac…` 단일 상태였고, 각 실행의 전후 원본은 모두
  양성이었다. 모든 단계에서 `GL_NO_ERROR`를 확인했다
- 1차 분리 결과: 스캔 법선 변형 제거, 패턴 높이 변형 제거, 패턴 상수화는 각각 양성이다.
  스캔 호출 전체 제거, 비파사드 BRDF만 유지, 비파사드 상수 색상/discard는 이번 표본에서
  대상 단독과 같았다. 원본과 동일 소스의 재컴파일 대조도 양성이었다
- 큰 폭의 축소인 `scan_brdf`와 그 법선 제거·albedo/ARM 단독·고정 레이어 변형 5종은
  모두 음성이었다. **스캔 호출을 남기는 것만으로 재현이 유지되지는 않는다.** 특히 이
  원거리 형상은 shader의 420m 세부 효과 범위 밖이므로, 실제 텍스처 샘플링을 원인으로
  확정할 수 없다. 컴파일된 전체 경로와 계산 입력의 영향도 분리해야 한다
- 최종 `concrete_scan`은 일반 재질 분기에서 콘크리트 패턴·표면 높이·스캔 호출·색 변화·
  반사·발광을 유지하고, 도로·잔디·흙·교량·투과광 분기를 제거했다. include 확장 후
  fragment 소스가 **36,866 → 31,062바이트(약 15.7% 감소)**이며 양 draw 순서 모두 양성이다
  - 트리거→대상: 시간 변동 **47/1,024**, 대상 단독과 다른 상태 **977/1,024**
  - 대상→트리거: 시간 변동 **5/1,024**, 대상 단독과 다른 상태 **1,019/1,024**
  - 두 상태는 기존 `c85f6aac…` / `ec5e576a…`, 최대 RGB 차이 `1.572609e-3`로 동일하다
- 법선·패턴 높이 제거와 패턴 상수화를 동시에 적용한
  `combined_no_normal_relief_pattern`도 양성이었다(시간 변동 **51·7/1,024**).
  이는 다음 축소의 출발점이며 최소성이나 단일 원인을 증명한 것은 아니다
- 산출물: `fragment_reduction_intel_arc_140v.json`,
  `fragment_reduction_scan_intel_arc_140v.json`,
  `fragment_reduction_concrete_intel_arc_140v.json`.
  단계별 저장, 입력·셰이더 해시, 실효 scissor, 프로그램 재사용 여부, 대조군 일치 여부를 기록한다
- 품질 변화: 없음. 새 프로그램은 진단 앱에서만 생성하고 해제한다. 생산 셰이더·렌더 경로와
  과거 측정 파일은 변경하지 않았다. 독립 제출용 재현과 드라이버 원인 확정은 아직 미완료다
- 검증: 전체 pytest **794 passed, 3 skipped**. 15개 변형의 실제 GL 컴파일·링크, 파사드
  본문 보존, 축소 지점 변경 시 실패, 대조군 변동 시 판정 보류를 테스트한다
- 다음 작업: **V0-15C 양성 축소본의 의존성 분리**. `concrete_scan`을 기준으로 위의 복합
  제거를 합치고, 파사드의 스캔 경로·미사용 helper를 단계적으로 줄인다. 양성을 유지하는
  지점에서 실제 uniform·지형/재질 텍스처를 고정해 앱 초기화 없는 재현으로 옮긴다

### 2026-09-11 — V0-15C 스냅샷 의존성 분리와 독립 재현 성립

- V0-15B의 양성 축소본을 **앱 초기화 없이 재현**하는 단계를 끝냈다. GPU 자원(9정점,
  생산 vertex shader, 3개 fragment 프로그램, uniform, terrain·scanned 텍스처 전 레벨)을
  이식 가능한 ZIP으로 직렬화하고, 새 standalone 컨텍스트에서 복원해 측정한다
- 도구 3종을 추가했다. `tools/facade_snapshot.py`는 simulator를 임포트하지 않는 순수
  데이터·측정 헬퍼이고, `capture_facade_snapshot.py`가 캡처와 인컨텍스트 왕복 대조를,
  `replay_facade_snapshot.py`가 독립 재생을 맡는다. 배열 텍스처 mip은 ModernGL이
  레벨 0만 노출하므로 명시적 pack/unpack 정렬로 전 레벨을 옮기고 **바이트 단위로
  되읽어 검사**한다(RGB 1×1 꼬리 레벨 포함)
- 스냅샷: `local_reference/facade_v0_15c.zip`, **32,141,327바이트**,
  sha256 `8d1a2542…`, 17개 파일. 보고서의 기록값과 바이트·해시가 모두 일치함을 확인했다
- **독립 재현 성립**: 프로젝트 밖 디렉터리에 `facade_snapshot.py`,
  `replay_facade_snapshot.py`, ZIP **세 파일만** 두고 실행했고 `simulator_modules_loaded`가
  **빈 배열**임을 스크립트가 자체 검사한다. 3개 프로그램 × 4조건 × 1,024회 측정 결과
  - `target_only`와 `target_only_after`는 **전부 1024/1024 단일 상태** `c85f6aac…`.
    측정 전후 대조가 모두 안정이므로 트리거 효과 판정이 유효하다
  - 트리거를 넣으면 두 상태가 나온다. 대상 단독과 다른 표본 수는 original 836·840,
    concrete_scan 994·1,010, concrete_combined 971·965(각각 trigger→target, target→trigger)
  - 최대 RGB 차이는 세 프로그램·양 순서 모두 **`1.572609e-03`**으로, 인앱 측정값과 동일하다
  - 모든 단계에서 `GL_NO_ERROR`
- 같은 Intel Arc 140V / `32.0.101.8626`이다. live·인컨텍스트 왕복·독립 재생 **세 경로
  모두** `trigger_effect_observed=true`이며, 직렬화가 현상을 보존한다
- **아직 증명하지 않은 것**: 이것은 재현이지 드라이버 결함의 증명이 아니다. 유한 표본은
  안정성·필요조건·원인을 확정하지 못하고, 정확한 자원 복원이 이전 드라이버·컨텍스트
  이력까지 재현하는 것은 아니다. 두 보고서에 한계로 명시했다
- 품질 변화: 없음. 생산 셰이더·렌더 경로·장면 데이터 변경 0건. 새 프로그램과 컨텍스트는
  진단 경로에서만 만들고 해제한다
- 검증: 전체 pytest **800 passed, 0 skipped**. 번들 무결성·덮어쓰기 거부·텍스처 복원
  왕복을 테스트한다
- 산출물: `snapshot_capture_intel_arc_140v.json`(live + 인컨텍스트 왕복),
  `snapshot_replay_intel_arc_140v.json`(독립 재생)
- **외부 제출의 남은 제약**: 번들에 원본 텍스처 픽셀이 들어 있다. `scanned` 4종은
  Poly Haven **CC0**라 재배포에 문제가 없으나, `terrain` 텍스처는 공식 국내 자료에서
  파생되어 재배포 조건이 확인되지 않았다. 그래서 ZIP은 커밋하지 않고 로컬에만 둔다
- 다음 작업: **V0-15D 재배포 가능 번들**. terrain 텍스처를 합성 데이터로 치환해도 양성이
  유지되는지 확인한다. 유지되면 CC0 재질 + 합성 terrain 조합으로 외부 제출 가능한
  번들이 되고, 유지되지 않으면 terrain 내용이 재현에 필수라는 사실 자체가 새 단서다

### 2026-09-11 — V0-15D terrain 치환으로 재배포 제약 축소

- V0-15C 번들이 공유되지 못한 유일한 이유는 1024×1024 terrain 높이장이었다. 그런데
  그 대부분은 읽히지도 않는다. 9정점이 닿는 texel을 번들 자체에서 계산했다 —
  좌표를 상수로 박지 않고 `vertices.bin`과 `terrain_bounds` uniform에서 유도한다
- **1,048,576개 중 70개(0.0067%)** 만 도달 가능하다. ±2 halo는 바이리니어 2×2 블록과
  `textureOffset`의 ±1 십자를 모두 덮는다
- 셰이더 경로도 확인했다. `follows_terrain`은 surface 2.5~7.5 또는 13.5~15.5에서만
  참인데 대상은 0, 트리거는 12라 **둘 다 거짓**이다. 따라서 지형 법선 경로는 타지 않고,
  `base_height`는 `world_position = animated_position + vec3(0, base_height, 0)` 한 곳에서만
  쓰인다. halo 밖 치환은 **정점을 전혀 움직이지 않는다**(도구가 가정하지 않고 단언한다)
- `tools/substitute_facade_terrain.py`로 halo 밖을 결정적 합성 램프로 채웠다.
  **남은 원본은 70 texel · 280바이트**이고 1,048,506개 texel이 바뀌었다
- **치환본도 동일하게 재현된다.** 프로젝트 밖에서 `simulator` 모듈 0개로 재생한 결과
  - 대조군 `target_only`·`target_only_after`는 전부 **1024/1024 단일 상태**
  - 대상 단독과 다른 표본: original 833·835, concrete_scan 1,009·1,001,
    concrete_combined 965·953
  - 두 상태 해시 `c85f6aac…`/`ec5e576a…`와 최대 RGB 차이 **`1.572609e-03`**이
    원본 번들·인앱 측정과 **모두 동일**하다
- 재배포 관점의 현재 구성: 재질은 Poly Haven **CC0**, terrain은 합성 램프 + 원본
  70개 높이값(280바이트)이다. 법적 판단은 하지 않고 구성과 수량만 기록한다
- **하지 않은 것**: 마지막 280바이트까지 없애려면 `base_height`를 정점 Y에 미리 더하고
  terrain을 전부 0으로 두면 된다. 다만 CPU 바이리니어 값이 GPU 값과 1 ULP 다를 수 있고,
  이 잔차는 그 정도에 민감할 수 있어 위음성을 만들 위험이 있다. 이득 대비 위험이 맞지
  않아 시도하지 않았다
- 품질 변화: 없음. 생산 셰이더·렌더 경로·장면 데이터 변경 0건. 치환은 로컬 번들에만 적용된다
- 검증: 전체 pytest **805 passed, 0 skipped**. 신규 5개 — 유지 집합이 상수가 아니라
  번들에서 유도됨, halo가 샘플링 이웃을 덮음, 세 가지 fill 모두에서 유지 texel 불변,
  결정성, 미지 fill 거부
- 산출물: `terrain_substitution_intel_arc_140v.json`,
  `snapshot_replay_substituted_intel_arc_140v.json`
- 다음 작업: **V0-15E 외부 제출 판단**. 재현이 성립하고 번들이 사실상 합성으로 바뀌었으므로,
  (a) Intel에 드라이버 이슈로 제출할지, (b) 그 전에 다른 GPU·드라이버에서 음성임을
  확인해 하드웨어 특정성을 보강할지 사용자가 정할 사안이다. 어느 쪽도 임의로 진행하지 않는다

### 2026-09-11 — V0-15E 다른 구현에서의 음성 대조군

- 제출 전 하드웨어 특정성을 보강하기로 하고, **동일 번들을 다른 OpenGL 구현에서 돌렸다**
- 이 장비에는 GPU가 Intel Arc 140V 하나뿐이라 외장 GPU 대조는 불가능하다. 대신 이미
  설치돼 있던 Qt 번들 소프트웨어 OpenGL(`opengl32sw_w.dll`)이 **Mesa 21.1.3 Gallium
  llvmpipe**임을 확인하고, 사용자 파이썬을 건드리지 않도록 스크래치패드에 격리 venv를
  만들어 거기서만 선로드했다. 드라이버 설치·시스템 변경은 하지 않았다
  - `opengl32.dll`은 KnownDLLs가 아니지만 앱 디렉터리 배치만으로는 치환되지 않아,
    같은 base name으로 선로드해 전환했다. 전환 결과 GL 4.5 Core / Mesa 21.1.3
- **결과 — 동일 번들(sha256 `ad2df3e3…`), 동일 1,024회**

  | 구현 | 대상 단독과 다른 표본 | 상태 수 | 이방성 |
  |---|---:|---:|---|
  | Intel Arc 140V `32.0.101.8626` | **5,298 / 12,288** | 2 | 8.0 적용 |
  | Mesa 21.1.3 llvmpipe | **0 / 12,288** | **1** | 미지원(0.0) |

  llvmpipe에서는 3개 프로그램 × 4조건 전부 단일 상태이고 최대 RGB 차이가 모두 0이다.
  대조군은 양쪽 모두 안정이다
- **가드를 조용히 약화시키지 않았다.** llvmpipe는 이방성 확장을 아예 노출하지 않아
  캡처 상태를 복원할 수 없다. 기존 도구는 이때 하드 실패했는데, 이를 없애는 대신
  명시적 `--allow-sampler-mismatch`를 추가하고 **요청값·실제값·일치 여부를 보고서에
  기록**하도록 했다. 플래그 없이 Intel에서 돌리면 동작은 종전과 같다(일치 8.0, 가드 유효)
- 한계를 분명히 한다
  - llvmpipe는 **소프트웨어 래스터라이저이지 두 번째 하드웨어 벤더가 아니다.** 이
    음성은 "GL 명세가 이 거동을 요구하지 않으며 독립 구현은 결정적"임을 보이지만,
    다른 GPU 벤더의 음성보다는 약한 증거다
  - 이방성이 양쪽에서 다르다(8.0 대 미지원). V0-8에서 Intel 기준 8.0과 1.0이
    바이트 동일이었으므로 이 현상의 결정 요인은 아니지만, 완전한 동등 조건은 아니다
  - 절대 픽셀값은 구현마다 다르다(상태 해시가 다름). 비교 대상은 출력의 동일성이 아니라
    **"트리거 draw가 대상의 출력을 바꾸는가"** 이다
- 품질 변화: 없음. 생산 셰이더·렌더 경로·장면 데이터 변경 0건. venv와 DLL은 스크래치패드에만 있다
- 검증: 전체 pytest **810 passed, 0 skipped**. 신규 5개 — 두 실행이 같은 번들·같은
  반복수인지, 구현이 실제로 다른지, Intel 양성·llvmpipe 음성, llvmpipe 단일 상태,
  그리고 **샘플러 차이가 보고서에 공시되는지**(미공시 시 실패)
- 산출물: `negative_control_mesa_llvmpipe_21_1_3.json`,
  갱신된 `snapshot_replay_substituted_intel_arc_140v.json`
- 다음 작업: 하드웨어 벤더 대조가 남았다. 다른 GPU를 쓸 수 있게 되면 같은 번들·같은
  명령으로 한 번 돌리면 되고, 그 전까지 외부 제출 여부는 사용자 판단 사항이다

### 2026-09-11 — V0-16 캡처 게이트 복구: 공시된 장치 한정 허용

- **문제를 먼저 실측했다.** 이 장비에서 `water_reflection`을 4회 캡처해 6쌍을 비교하니
  **5/6이 실패**했다. 프로젝트의 1차 검증 계기가 대부분 빨간불이면 진짜 회귀를 잡을 수
  없다. 원인은 V0-15에서 규명한 두 잔차다
- **정정**: V0-5에서 "표시 프레임은 통과한다"고 했는데, 이는 잔차 B에만 해당한다.
  잔차 A(136픽셀, 행 357~387·열 525~536)는 **SDR 게이트도 실패시킨다**. B도 SDR에
  도달하며(8픽셀, 최대 25 코드값) 다만 허용 오차 안이라 통과하던 것이다 — 부재해서가
  아니다
- 해결은 한도를 올리는 것이 아니라 **더 좁은 예외**다. `assets/known_render_residuals.json`에
  잔차마다 시점·화면 영역·픽셀 수 상한·크기 상한과 드라이버 빌드를 적고, 비교는
  그 픽셀만 계량에서 빼되 **상한을 넘으면 실패**시킨다. 전역 한도
  `DEFAULT_HDR/SDR_REGRESSION_LIMITS`는 **손대지 않았다**
- 매니페스트에 GL 벤더·렌더러·버전을 기록하도록 했다. 이전에는 두 캡처가 같은 드라이버에서
  나왔는지조차 알 수 없었다. 허용은 **양쪽 캡처가 기록된 장치와 일치할 때만** 적용되고,
  두 캡처의 GL이 서로 다르면 비교 자체를 불일치로 보고한다
- **결과**: 새로 6회 캡처해 10쌍을 비교하니 **10/10 통과**다. 더 중요한 것은 허용 픽셀을
  제외한 뒤 `changed_pixel_count`가 **모든 쌍에서 0**이라는 점이다. 허용이 다른 오차를
  덮고 있지 않으며, 두 잔차가 차이의 전부였다
- 예외가 자라지 못하게 테스트로 묶었다(신규 10개): 영역 **밖** 차이는 여전히 실패,
  영역 안이라도 픽셀 수·크기 상한 초과 시 실패, 기록되지 않은 장치·시점에는 미적용,
  `allowances` 미지정 시 종전과 동일, 전역 한도 상수 불변, 그리고 각 잔차가 근거 파일을
  인용하고 상한이 실측값 이상인지 검사
- 이로써 **V0 완료 게이트 조건 1이 충족**됐다. 조건 2(모든 시각 커밋의 전후 보고서
  생성)는 도구는 있으나 강제가 없고, hook/CI 설치는 사용자 워크플로 결정이라 하지 않았다.
  V0 추정을 70%에서 85%로 올린다 — 남은 15%는 조건 2와, 사진 등록이 자료상 불가로
  판정된 부분이다
- 품질 변화: 없음. 생산 셰이더·렌더 경로·장면 데이터 변경 0건
- 검증: 전체 pytest **820 passed, 0 skipped**
- 다음 작업: 사용자 대기 항목(V2-2f 회신, V3-1 서비스 키·재빌드)이 풀리기 전까지는
  V2의 "카메라 이동 중 반짝임과 반복 타일 패턴 제거"가 남은 최대 독립 항목이다.
  V0-8에서 이 파사드가 1.5km에서 셀당 2픽셀 미만으로 표본화됨을 이미 확인했다

### 2026-09-11 — V2-3b 반짝임 원인 가설 3건 반증

- V2-3a 기준선을 근거로 수정을 시도했고, **세 가설이 모두 반증됐다.** 셰이더는
  되돌렸고 검증되지 않은 변경은 남기지 않았다
- **가설 1 — 창 격자 per-cell 난수의 나이키스트 미달**: 셀 발자국이 2픽셀 미만이면
  per-cell 난수를 기댓값으로 수렴시키는 `cell_detail` 페이드를 넣었다. 기댓값은 상수로
  적지 않고 같은 경계값에서 유도했다(`E[step(t,u)]=1-t`, `E[smoothstep(a,b,u)]=1-(a+b)/2`).
  결과는 **facade_landmark 62.17% → 62.17%(무변화)**, water_reflection 17.11% →
  **17.58%(악화)**. 이 시점들에서는 페이드가 거의 발동하지 않는다
- **가설 2 — 기하 실루엣 에일리어싱**: `facade_landmark`의 커버리지는 **100%**다.
  하늘이 없어 실루엣 픽셀이 **0개**이고 오차는 전부 내부 표면이다
- **가설 3 — 스캔 재질 노멀맵**: 노멀 섭동 한 줄을 진단용으로 끄고 측정했더니
  mean·p95·비율이 **바이트 단위로 동일**했다. 이 표면들에는 스캔 재질이 적용되지 않는다.
  측정 후 즉시 되돌렸다
- **재현성 확인**: 되돌린 뒤 water_reflection이 기준선과 정확히 일치했다
  (mean 1.9977e-04, p95 3.1859e-04, 17.11%)
- **지표의 한계를 새로 발견했다 — 이것이 이번 턴의 가장 중요한 산출이다.**
  발자국 기반 LOD 페이드는 초해상 기준 지표로 **판정할 수 없다.** 기준 쪽은 발자국이
  4배 작아 페이드가 덜 걸린 더 상세한 영상이 되므로, 1×에서 올바르게 수렴시킬수록
  차이는 오히려 커진다. 가설 1에서 water_reflection이 악화된 것이 바로 이 효과이며,
  이를 모른 채 다음 시도를 했다면 옳은 수정을 "악화"로 오판했을 것이다
- 따라서 반짝임 판정에는 **카메라 이동 중 시간축 안정성**(V0-2의 `temporal_stability`)을
  써야 한다. 거기서는 LOD 수렴이 점수를 개선시킨다
- 품질 변화: 없음. 셰이더·렌더 경로 최종 변경 0건(진단 변경은 모두 되돌림)
- 검증: 전체 pytest **820 passed, 0 skipped**(코드 변경 없음, 직전 상태 유지)
- 산출물: `negative_cell_detail_fade.json`, `revert_reproducibility_check.json`,
  갱신된 `aliasing_v2/README.md`
- **부수 발견 — 저장소 결함 하나를 잡았다.** 진단 셰이더를 `git checkout`으로 되돌리자
  파일이 **CRLF로 복원**되어 체크섬 고정 테스트가 실패했다. 원인은 `core.autocrlf=true`
  인데 `.gitattributes`가 **없다**는 것이다. 즉 **Windows에서 새로 clone하면** 셰이더를
  해시로 잠근 테스트가 곧바로 깨진다 — 내 되돌리기가 그 경로를 우연히 밟았을 뿐이다
  - 셰이더 3종(`.frag`/`.vert`/`.glsl`)만 `eol=lf`로 고정했다. 저장소의 다른 추적 텍스트
    (README·여러 asset 등)는 이미 CRLF이고 체크섬 대상이 아니므로, 전체 정규화는
    실증된 결함의 수정이 아니라 저장소 전면 재작성이라 **임의로 하지 않았다**
  - 회귀 3개를 넣었다: 정책이 선언돼 있는지, 잠긴 소스가 해시와 **블롭 양쪽**에 일치하는지
    (같은 텍스트·다른 개행이 바로 이 실패 모드다), 잠긴 셰이더에 CRLF가 없는지
- 다음 작업: (1) 카메라 이동 경로 기반 시간축 반짝임 지표를 만들어 LOD 수정을 판정할 수
  있게 한다. (2) `facade_landmark`의 62%가 실제로 어디서 오는지는 추측을 멈추고
  `cells_per_pixel` 등 중간량을 셰이더에서 직접 출력해 분포를 본다 — V0-15에서 잔차를
  좁힐 때 통했던 방법이다
